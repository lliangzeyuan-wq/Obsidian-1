---
data: 2026-03-29
---
### 接下来按「**核心功能闭环 → 安全加固 → 部署上线 → 体验优化**」四步走，让产品真正具备上线能力：

---

## 第一阶段：核心功能闭环（先把「用户完整使用路径」补全）

现在消息已经持久化了，但还有很多功能断点，先补全最基础的用户操作：

### 1. 个人资料编辑（用户能改自己的信息）

- **后端**：新增 `/api/profile/update` 接口，接收用户修改的姓名、专业、标签等，更新 `users` 表
- **前端**：在「我的」页面加「编辑资料」按钮，弹出表单提交修改
- **关键**：只允许用户修改自己的资料（后续通过 token 鉴权限制）

### 2. 邀请处理（接受 / 拒绝搭子邀请）

- **后端**：新增 `/api/invites/handle` 接口，接收邀请 ID 和操作（`accept`/`reject`），更新 `invites` 表的 `status` 字段
- **前端**：新增「我的邀请」页面，展示收到的邀请，提供「接受」「拒绝」按钮
- **联动**：接受邀请后，双方自动加入聊天列表，可直接发起对话

### 3. 真实登录 / 注册（告别模拟数据）

- **后端**：
    
    - 注册接口 `/api/register`：将新用户写入 `users` 表，**密码加密存储**（先用 MD5，后续升级为 bcrypt）
    - 登录接口 `/api/login`：查询 `users` 表验证账号密码，生成真实 token（比如 UUID + 时间戳）
    
- **前端**：登录 / 注册页对接真实接口，将 token 存入 `localStorage`，后续所有请求携带 token

---

## 第二阶段：安全加固（防止上线后被攻击）

现在的代码有明显安全风险，必须先修复：

### 1. 防 SQL 注入

把所有用 `sprintf` 拼接 SQL 的地方，**改成 MySQL 预处理语句**（`mysql_stmt_init`/`mysql_stmt_prepare`），避免用户输入恶意 SQL 破坏数据库。

### 2. 接口鉴权

所有需要登录的接口（发消息、发邀请、改资料），先验证请求头里的 token：

- 后端新增 `verifyToken()` 函数，检查 token 是否有效
- 未登录用户直接返回 401 错误，禁止访问敏感接口

### 3. 跨域安全

把 `Access-Control-Allow-Origin: *` 改成**你的前端域名**（比如 `https://your-campus-app.com`），防止陌生网站盗用接口。

---

## 第三阶段：部署与运维（让服务 7×24 小时跑起来）

### 1. 后端服务后台运行

- 用 `nohup ./server &` 让服务在后台运行，关闭终端也不停止
- 进阶：写 `systemd` 服务文件（`/etc/systemd/system/campus-social.service`），让服务开机自启、崩溃自动重启

### 2. 日志与监控

- 把后端的 `cout` 输出改成**写日志文件**（用 `fstream` 或轻量日志库），记录请求、错误信息，方便排查问题
- 简单监控：用 `curl` 定时访问健康检查接口，服务挂了发邮件 / 短信告警

### 3. MySQL 数据安全

- 定时备份：用 `crontab` 每天执行 `mysqldump` 备份数据库，防止数据丢失
- 权限最小化：给后端 MySQL 账号只授权 `campus_social` 库的读写权限，不要用 root 账号直连

### 4. 服务器与域名

- 防火墙开放端口：`sudo ufw allow 8080/tcp`（或 80/443）
- 有域名的话：解析到服务器 IP，用 Nginx 反向代理（80/443 → 8080），配置 HTTPS（Let's Encrypt 免费证书）

---

## 第四阶段：用户体验优化（让产品更好用）

### 1. 消息实时推送

- 后端集成 WebSocket（比如 `libwebsockets`），实现新消息实时推送，不用刷新页面就能收到消息
- 前端对接 WebSocket，监听新消息，自动渲染到聊天框

### 2. 体验细节优化

- 消息时间格式化：显示「刚刚」「1 分钟前」「今天 18:47」，更友好
- 消息已读状态：对方查看消息后，发送方看到「已读」标识
- 加载与错误提示：发消息时显示 loading 动画，网络失败弹出「发送失败，请重试」
- 头像展示：`users` 表加 `avatar` 字段，前端显示用户头像，更有辨识度

---

## 🎯 先从哪一步开始？

**建议先做「个人资料编辑 + 真实登录注册」**，这是用户最基础的需求，补全后产品就有了「注册→登录→改资料→匹配→发邀请→聊天」的完整闭环，再逐步推进安全和部署。

需要我先帮你把「个人资料编辑」的后端接口 + 前端代码写出来吗？