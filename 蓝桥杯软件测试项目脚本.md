
@echo off

setlocal enabledelayedexpansion
rem 设置临时系统变量
set "SYSTEM_PATH=%SystemRoot%\system32"
set "CURRENT_PATH=%PATH%"
set "PATH=%SYSTEM_PATH%;%SYSTEM_PATH%\wbem;%CURRENT_PATH%"   

:: 获取操作系统版本
for /f "tokens=4-5 delims=. " %%i in ('ver') do set VERSION=%%i.%%j
for /f "tokens=2 delims==" %%i in ('wmic os get caption /value ^| findstr "Caption"') do set OS=%%i

:: 根据操作系统版本设置字符集
IF not "%VERSION%"=="6.1" (
   chcp 936 > nul  
) 

rem 获取当前工作目录路径
set "workDir=%CD%"
set JDK_HOME=.\jdk
REM 设置 JDK 的安装路径
IF "%PROCESSOR_ARCHITECTURE%"=="AMD64" (
   echo 你的操作系统是64位 Windows %VERSION% 
) ELSE (
   echo 你的操作系统是32位 Windows %VERSION% 
)

REM 设置 JAR 文件的路径和文件名
set JAR_FILE=.\mocksoftest.jar
REM 获取批处理脚本所在的目录
set SCRIPT_DIR=%~dp0
REM 拼接完整路径
set JDK_PATH=%SCRIPT_DIR%%JDK_HOME%
set JAR_PATH=%SCRIPT_DIR%%JAR_FILE%
REM 安装 JDK
set PATH=%JDK_PATH%\bin;%PATH%

REM 检测并停止已经启动的 JAR 程序
tasklist /nh /fi "IMAGENAME eq java.exe" | find /i "java.exe" > nul && (
echo 备测系统后台程序已经在运行，正在停止...
taskkill /f /im java.exe > nul
)

REM 运行 JAR 程序
cd %SCRIPT_DIR%
start "被测系统" java -jar %JAR_PATH%
echo 等待 15 秒后自动访问首页，或手动在浏览器中访问 http://localhost:8090
timeout /t 15 /nobreak
start  http://localhost:8090
pause