---
data: 2026-02-18
---
### 使得外界无法使用构造函数和拷贝构造函数、拷贝赋值操作符重载
- 从而保证一个类只能有一个对象
**方法1**
- 将他们设为delete，即禁用
```cpp
#include<iostream>
using namespace std;
class TaskQueue {
public:
	TaskQueue() = delete;
	TaskQueue(const TaskQueue& t) = delete;
private:
};
```
**方法2**
- 设为私有
```cpp
#include<iostream>
using namespace std;
class TaskQueue {
public:
private:
	TaskQueue() {};
	TaskQueue(const TaskQueue& t) {};
};
```
**方法3**
- 私有的默认构造，和方法2的写法差不多
```cpp
#include<iostream>
using namespace std;
class TaskQueue {
public:
private:
	TaskQueue() = default;
	TaskQueue(const TaskQueue& t) = default;
};
```