---
data: 2026-01-19
---


- 在c语言中使用需要包含#include< math.h >  ,在c++语言中使用需要包含#include< cmath>
- `sqrt(数值)` ：平方根函数 → **参数 double，返回值 必为 double**（如`sqrt(4.0)=2.0`、`sqrt(4)=2.0`）
- `pow(数值,次方)` ：幂函数 → **返回值 必为 double**
- `abs(数值)` ：计算整型绝对值 → 返回值 `int`
- `fabs(数值)` ：计算浮点型绝对值 → 返回值 `double`