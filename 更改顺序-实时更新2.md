### 问题定位与解决步骤

从你的虚拟机终端可以看到，**内网 IP 为 `192.168.198.131`**，这是宿主机访问虚拟机后端的关键地址。之前的 `signal is aborted` 错误，本质是宿主机访问 `127.0.0.1:8080` 时，请求根本没发到虚拟机里的后端。

---

### ✅ 第一步：修改前端请求地址

打开你的 `api.js` 文件，将 `API_CONFIG.baseUrl` 替换为虚拟机 IP：

javascript

运行

```
const API_CONFIG = {
    baseUrl: 'http://192.168.198.131:8080', // 替换为你的虚拟机IP
    timeout: 10000,
    retryTimes: 3,
    useMock: false
};
```

---

### ✅ 第二步：验证后端连通性

在**宿主机**的终端（或浏览器地址栏）执行测试：

bash

运行

```
curl http://192.168.198.131:8080
```

✅ 预期结果：返回 `{"success":true,"message":"请求成功"}`，说明宿主机能正常访问虚拟机后端。

---

### ✅ 第三步：测试注册功能

1. 保存 `api.js` 文件，在浏览器按 `Ctrl+F5` 强制刷新页面（清空缓存）。
2. 确保虚拟机里的后端服务仍在运行（终端显示 `✅ 服务器启动成功！端口：8080`）。
3. 再次点击「注册」按钮：
    
    - 后端终端会打印请求日志
    - 前端弹出「注册成功 / 学号已存在」提示，问题解决 ✅
    

---

### 💡 补充：如果仍无法访问

1. **检查虚拟机防火墙**：临时关闭防火墙测试
    
    bash
    
    运行
    
    ```
    sudo ufw disable
    # 或仅开放8080端口
    sudo ufw allow 8080/tcp
    ```
    
2. **确认虚拟机网络模式**：确保虚拟机网络为「NAT 模式」或「桥接模式」，避免「仅主机模式」导致隔离。

---

### 最终效果

修改后，前端请求会直接发送到虚拟机的后端服务，不再出现 `signal is aborted` 错误，注册 / 登录功能即可正常使用。

需要我帮你把修改后的完整 `api.js` 代码直接生成出来吗？

思考

编程

图像生成

帮我写作

翻译

视频生成

更多