### 题根因找到了！

你的 `Authorization` 头是**正确的**（`Bearer token_1774849703`），但后端 `verifyToken` 函数验证失败，所以返回「请先登录」。

---

### ✅ 修复后端 `server.cpp` 的 `verifyToken` 函数

把你原来的 `verifyToken` 函数**整个替换**成下面这段代码：

cpp

运行

```
// 验证Token是否有效，返回用户ID
string verifyToken(MYSQL* conn, const string& token) {
    if (token.empty()) return "";
    
    // 1. 先转义 token，避免 SQL 注入/语法错误
    char escaped_token[1024];
    mysql_real_escape_string(conn, escaped_token, token.c_str(), token.size());
    
    // 2. 拼接安全的 SQL 查询
    char sql[512];
    sprintf(sql, "SELECT id FROM user WHERE token='%s'", escaped_token);

    if (mysql_query(conn, sql)) {
        cerr << "SQL 执行失败: " << mysql_error(conn) << endl;
        return "";
    }
    
    MYSQL_RES* res = mysql_store_result(conn);
    if (!res) {
        cerr << "获取结果集失败: " << mysql_error(conn) << endl;
        return "";
    }

    MYSQL_ROW row = mysql_fetch_row(res);
    string user_id = row ? row[0] : "";
    mysql_free_result(res);
    
    if (user_id.empty()) {
        cerr << "未找到对应 token 的用户: " << token << endl;
    }
    return user_id;
}
```

---

### ✅ 再修复「Authorization 头截取」的代码

在 `handleClient` 函数里，找到提取 `token` 的代码，改成这样：

cpp

运行

```
// 提取请求头中的Token
string token = "";
size_t auth_pos = request.find("Authorization: Bearer ");
if (auth_pos != string::npos) {
    // "Authorization: Bearer " 实际长度是 22（包括最后的空格）
    token = request.substr(auth_pos + 22); 
    size_t end_pos = token.find("\r\n");
    if (end_pos != string::npos) token = token.substr(0, end_pos);
}
```

---

### 🚀 最后一步

1. 保存 `server.cpp` 修改
2. **重新编译运行后端**
3. 前端刷新页面 → 重新登录 → 填资料 → 点「完成」

这次后端就能正确验证你的 `token`，资料会正常保存到 MySQL，不会再提示「请先登录」了！