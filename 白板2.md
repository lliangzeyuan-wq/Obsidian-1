# 1. 服务端代码 `server.c`（带详细注释）

c

运行

```
// 包含标准输入输出库，用于printf、perror等打印函数
#include <stdio.h>
// 包含标准库，用于malloc、free、exit等内存/程序退出函数
#include <stdlib.h>
// 包含字符串处理库，用于memset、strcpy、strcmp等字符串操作
#include <string.h>
// 包含Unix标准库，用于close、read、write等文件操作函数
#include <unistd.h>
// 包含Socket网络编程核心头文件
#include <sys/socket.h>
// 包含网络地址结构体头文件
#include <netinet/in.h>
// 包含多线程编程头文件（pthread线程库）
#include <pthread.h>

// 定义服务器监听的端口号（校园交友系统固定端口）
#define SERV_PORT 9527
// 定义最大在线用户数量
#define MAX_USER 100

// -------------------------- 全局数据结构定义 --------------------------
// 用户结构体：存储每个在线用户的信息（学号+客户端连接标识）
// 这是校园交友系统的核心数据结构
typedef struct {
    char id[20];      // 用户学号（唯一标识）
    int fd;           // 客户端socket文件描述符（用于收发消息）
} User;

// 全局用户列表：存储所有在线的用户
User user_list[MAX_USER];
// 在线用户数量计数器
int user_count = 0;
// 互斥锁：保护全局用户列表（多线程同时修改数据时防止混乱）
pthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;

// -------------------------- 函数声明 --------------------------
// 线程处理函数：专门处理一个客户端的所有请求
void *handle_client(void *arg);
// 错误处理函数：打印错误信息并退出程序
void sys_err(const char *str);

// -------------------------- 主函数（服务器入口） --------------------------
int main() {
    int lfd;                          // 监听socket：负责等待客户端连接
    struct sockaddr_in serv_addr;     // 服务器地址结构体
    socklen_t clie_addr_len = sizeof(serv_addr);  // 客户端地址长度
    pthread_t tid;                    // 线程ID：用于创建新线程

    // 1. 创建Socket（流式套接字，TCP协议）
    lfd = socket(AF_INET, SOCK_STREAM, 0);
    if (lfd == -1) sys_err("socket创建失败");

    // 2. 设置端口复用：解决"Address already in use"报错
    int opt = 1;
    setsockopt(lfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

    // 3. 配置服务器地址信息
    serv_addr.sin_family = AF_INET;         // 使用IPv4协议
    serv_addr.sin_addr.s_addr = htonl(INADDR_ANY);  // 监听本机所有IP
    serv_addr.sin_port = htons(SERV_PORT);   // 绑定端口9527

    // 4. 绑定IP和端口到监听socket
    if (bind(lfd, (struct sockaddr *)&serv_addr, sizeof(serv_addr)) == -1)
        sys_err("绑定地址失败");

    // 5. 开始监听客户端连接（最大等待队列128）
    if (listen(lfd, 128) == -1) sys_err("监听失败");
    printf("校园交友服务器启动成功，端口：%d，等待客户端连接...\n", SERV_PORT);

    // 6. 死循环：持续接收新客户端连接
    while (1) {
        // 动态分配内存：存储客户端连接描述符（解决线程传参警告）
        int *cfd_ptr = (int*)malloc(sizeof(int));
        // 接受客户端连接（阻塞等待，直到有客户端连接）
        *cfd_ptr = accept(lfd, (struct sockaddr *)&serv_addr, &clie_addr_len);

        // 接受连接失败处理
        if (*cfd_ptr == -1) {
            perror("accept失败");
            free(cfd_ptr);  // 释放内存
            continue;
        }

        // 打印新用户连接信息
        printf("新用户连接，socket=%d\n", *cfd_ptr);

        // 7. 创建新线程：单独处理这个客户端（实现多人同时在线）
        pthread_create(&tid, NULL, handle_client, (void*)cfd_ptr);
        // 线程分离：线程结束后自动释放资源，无需手动回收
        pthread_detach(tid);
    }

    // 关闭监听socket（理论上不会执行到这里）
    close(lfd);
    return 0;
}

// -------------------------- 线程处理函数（核心业务逻辑） --------------------------
// 功能：单独为一个客户端提供服务，处理登录、收发消息
void *handle_client(void *arg) {
    // 解引用：获取客户端socket描述符
    int cfd = *(int*)arg;
    // 释放动态分配的内存（防止内存泄漏）
    free(arg);

    char buf[1024];    // 数据缓冲区：存储收发的消息
    int n;             // 接收数据的长度
    char user_id[20];  // 存储当前客户端的学号

    // -------- 第一步：接收客户端发送的学号，完成登录 --------
    memset(buf, 0, sizeof(buf));  // 清空缓冲区
    // 读取客户端发送的学号
    n = read(cfd, buf, sizeof(buf));
    // 客户端断开连接处理
    if (n <= 0) {
        close(cfd);
        return NULL;
    }
    // 把接收到的学号复制到user_id变量
    strcpy(user_id, buf);
    printf("用户 %s 登录成功，socket=%d\n", user_id, cfd);

    // -------- 第二步：将用户加入在线用户列表（加锁保护） --------
    pthread_mutex_lock(&mutex);  // 加锁：防止多线程同时修改列表
    strcpy(user_list[user_count].id, user_id);  // 存储学号
    user_list[user_count].fd = cfd;             // 存储客户端socket
    user_count++;  // 在线用户数+1
    pthread_mutex_unlock(&mutex);  // 解锁

    // -------- 第三步：循环处理客户端消息 --------
    while (1) {
        memset(buf, 0, sizeof(buf));  // 清空缓冲区
        // 读取客户端发送的消息
        n = read(cfd, buf, sizeof(buf));

        // 客户端断开连接/读取失败处理
        if (n <= 0) {
            printf("用户 %s 断开连接，socket=%d\n", user_id, cfd);

            // 从在线用户列表中移除该用户（加锁）
            pthread_mutex_lock(&mutex);
            for (int i=0; i<user_count; i++) {
                // 找到当前用户的位置
                if (user_list[i].fd == cfd) {
                    // 后面的用户向前覆盖，删除当前用户
                    for (int j=i; j<user_count-1; j++) {
                        user_list[j] = user_list[j+1];
                    }
                    user_count--;  // 在线用户数-1
                    break;
                }
            }
            pthread_mutex_unlock(&mutex);  // 解锁

            close(cfd);  // 关闭客户端连接
            break;       // 退出循环，线程结束
        }

        // 打印收到的消息（绑定用户学号，区分用户）
        printf("收到用户 %s 的消息：%s\n", user_id, buf);
        // 回显消息：把消息发回客户端（测试用）
        write(cfd, buf, n);
    }
    return NULL;
}

// -------------------------- 错误处理函数 --------------------------
void sys_err(const char *str) {
    perror(str);   // 打印系统错误信息
    exit(1);      // 退出程序
}
```

# 2. 客户端代码 `client.c`（带详细注释）

c

运行

```
// 包含标准输入输出库
#include <stdio.h>
// 包含标准库
#include <stdlib.h>
// 包含字符串处理库
#include <string.h>
// 包含Unix标准库
#include <unistd.h>
// 包含Socket网络编程核心头文件
#include <sys/socket.h>
// 包含网络地址结构体
#include <netinet/in.h>
// 包含IP地址转换函数
#include <arpa/inet.h>

// 服务器端口号（必须和服务端一致）
#define SERV_PORT 9527

// -------------------------- 错误处理函数 --------------------------
void sys_err(const char *str) {
    perror(str);
    exit(1);
}

// -------------------------- 主函数（客户端入口） --------------------------
int main() {
    int cfd;                          // 客户端socket：用于和服务器通信
    struct sockaddr_in serv_addr;     // 服务器地址结构体
    char buf[1024];                   // 数据缓冲区
    int n;                            // 接收数据长度

    // 1. 创建客户端Socket（TCP流式套接字）
    cfd = socket(AF_INET, SOCK_STREAM, 0);
    if (cfd == -1) sys_err("客户端socket创建失败");

    // 2. 配置服务器地址信息
    serv_addr.sin_family = AF_INET;         // IPv4协议
    serv_addr.sin_port = htons(SERV_PORT);   // 服务器端口9527
    // 服务器IP：127.0.0.1（本机回环地址，本地测试用）
    inet_pton(AF_INET, "127.0.0.1", &serv_addr.sin_addr);

    // 3. 连接服务器
    if (connect(cfd, (struct sockaddr *)&serv_addr, sizeof(serv_addr)) == -1)
        sys_err("连接服务器失败");

    // -------------------------- 核心功能：学号登录 --------------------------
    printf("请输入你的学号：");
    scanf("%s", buf);          // 读取用户输入的学号
    write(cfd, buf, strlen(buf));  // 发送学号到服务器，完成登录
    printf("学号 %s 登录成功！\n", buf);

    // -------------------------- 循环收发消息 --------------------------
    while (1) {
        memset(buf, 0, sizeof(buf));  // 清空缓冲区
        printf("请输入消息：");
        scanf("%s", buf);             // 读取用户输入的消息

        // 发送消息到服务器
        write(cfd, buf, strlen(buf));

        // 输入quit，退出客户端
        if (strcmp(buf, "quit") == 0) break;

        // 接收服务器回显的消息
        memset(buf, 0, sizeof(buf));
        n = read(cfd, buf, sizeof(buf));
        if (n <= 0) break;  // 服务器断开连接，退出循环

        // 打印服务器返回的消息
        printf("服务器回显：%s\n", buf);
    }

    // 4. 关闭客户端socket
    close(cfd);
    return 0;
}
```

---

# 🔥 注释核心总结（你必须看懂的）

1. **服务端**
    
    - 负责**监听连接、多线程处理、用户登录、消息中转**
    - 互斥锁保护用户列表，保证多人在线不混乱