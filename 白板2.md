我给你把 **C++ 版的服务端 + 客户端** 加上**逐行超详细注释**，完全贴合你学过的黑马 C++、C 语言网络编程知识，**每一行代码都讲清楚作用**，你直接复制用，看懂就能开发项目！

# 一、服务端代码 `server.cpp`（完整版详细注释）

cpp

运行

```
// ==================== 头文件说明 ====================
// C++ 标准输入输出流（替代C语言printf，更简洁）
#include <iostream>
// C标准库头文件（兼容C语言的字符串、内存、系统函数）
#include <cstdio>   // 对应C语言 stdio.h
#include <cstdlib>  // 对应C语言 stdlib.h
#include <cstring>  // 对应C语言 string.h
#include <unistd.h>      // Linux系统调用：close/read/write
#include <sys/socket.h>  // Socket网络编程核心头文件
#include <netinet/in.h>  // 网络地址结构体（sockaddr_in）
#include <pthread.h>     // 多线程编程头文件

// ==================== 宏定义 ====================
// 服务器监听端口（校园交友系统固定端口）
#define SERV_PORT 9527
// 最大支持的在线用户数量
#define MAX_USER 100

// ==================== 数据结构定义 ====================
// 用户结构体：存储【在线用户】的核心信息（项目核心数据）
// 每个用户 = 学号(唯一标识) + 客户端连接标识(fd)
typedef struct {
    char id[20];   // 用户学号（登录用）
    int fd;        // 客户端socket文件描述符（收发消息用）
} User;

// ==================== 全局变量（多线程共享） ====================
// 在线用户列表：存储所有连接到服务器的用户
User user_list[MAX_USER];
// 在线用户计数器
int user_count = 0;
// 互斥锁：保护全局用户列表（多线程同时修改数据时防止错乱）
// C/C++ 用法完全一致
pthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;

// ==================== 函数声明 ====================
// 线程处理函数：专门为【一个客户端】提供服务
void *handle_client(void *arg);
// 统一错误处理函数
void sys_err(const char *str);

// ==================== 主函数（服务器入口） ====================
int main() {
    int lfd;                          // 监听Socket：专门等待客户端连接
    struct sockaddr_in serv_addr;     // 服务器IP/端口结构体
    socklen_t clie_addr_len = sizeof(serv_addr);  // 客户端地址长度
    pthread_t tid;                    // 线程ID：用于创建新线程

    // 1. 创建Socket（TCP流式套接字，和C语言完全一样）
    lfd = socket(AF_INET, SOCK_STREAM, 0);
    if (lfd == -1) sys_err("socket创建失败");

    // 2. 设置端口复用：解决"Address already in use"报错
    int opt = 1;
    setsockopt(lfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

    // 3. 配置服务器地址信息
    serv_addr.sin_family = AF_INET;         // 使用IPv4协议
    serv_addr.sin_addr.s_addr = htonl(INADDR_ANY);  // 监听本机所有IP
    serv_addr.sin_port = htons(SERV_PORT);   // 绑定端口9527

    // 4. 绑定IP和端口到监听Socket
    if (bind(lfd, (struct sockaddr *)&serv_addr, sizeof(serv_addr)) == -1)
        sys_err("bind绑定失败");

    // 5. 开始监听客户端（最大等待队列128）
    if (listen(lfd, 128) == -1) sys_err("listen监听失败");
    // C++ 打印：std::cout 替代 printf
    std::cout << "✅ 校园交友服务器启动成功！端口：" << SERV_PORT << "，等待用户连接..." << std::endl;

    // 6. 死循环：持续接收新客户端连接（服务器永久运行）
    while (1) {
        // C++ 语法：new 替代 C语言 malloc，更安全简洁
        // 动态分配内存：存储客户端连接标识（解决线程传参警告）
        int *cfd_ptr = new int;
        // 阻塞等待：接受客户端连接（有用户连接才会往下走）
        *cfd_ptr = accept(lfd, (struct sockaddr *)&serv_addr, &clie_addr_len);

        // 接受连接失败处理
        if (*cfd_ptr == -1) {
            perror("accept失败");
            delete cfd_ptr;  // C++ 释放内存：替代 free()
            continue;
        }

        // 打印新用户连接信息
        std::cout << "🔗 新用户连接，socket=" << *cfd_ptr << std::endl;

        // 7. 创建子线程：单独处理这个客户端（实现多人同时在线）
        // 把客户端连接标识传给线程函数
        pthread_create(&tid, NULL, handle_client, (void *)cfd_ptr);
        // 线程分离：线程结束后自动释放资源，无需手动回收
        pthread_detach(tid);
    }

    // 关闭监听Socket（理论上服务器永远不会执行到这里）
    close(lfd);
    return 0;
}

// ==================== 线程处理函数（核心业务逻辑） ====================
// 功能：独立处理一个客户端的【登录、收发消息、断开连接】
// 参数：客户端socket标识的指针（主线程传递过来）
void *handle_client(void *arg) {
    // 解引用：拿到真实的客户端连接标识
    int cfd = *(int *)arg;
    // C++ 释放动态分配的内存（防止内存泄漏，必须写）
    delete arg;

    char buf[1024];    // 数据缓冲区：存储收发的消息
    int n;             // 接收数据的长度
    char user_id[20];  // 存储当前用户的学号

    // -------- 第一步：接收客户端发送的学号，完成登录 --------
    // 清空缓冲区（防止脏数据）
    memset(buf, 0, sizeof(buf));
    // 读取客户端发送的学号
    n = read(cfd, buf, sizeof(buf));
    // 客户端未发送数据就断开，直接关闭连接
    if (n <= 0) {
        close(cfd);
        return NULL;
    }
    // 复制学号到变量中
    strcpy(user_id, buf);
    std::cout << "🎉 用户 " << user_id << " 登录成功！socket=" << cfd << std::endl;

    // -------- 第二步：将用户加入【在线用户列表】（加锁保护） --------
    // 加锁：同一时间只有一个线程能修改用户列表
    pthread_mutex_lock(&mutex);
    // 存储用户学号
    strcpy(user_list[user_count].id, user_id);
    // 存储用户连接标识
    user_list[user_count].fd = cfd;
    // 在线用户数+1
    user_count++;
    // 解锁：允许其他线程操作
    pthread_mutex_unlock(&mutex);

    // -------- 第三步：循环处理客户端消息 --------
    while (1) {
        memset(buf, 0, sizeof(buf));
        // 读取客户端发送的消息
        n = read(cfd, buf, sizeof(buf));

        // ============== 客户端断开连接处理 ==============
        if (n <= 0) {
            std::cout << "❌ 用户 " << user_id << " 断开连接，socket=" << cfd << std::endl;
            
            // 加锁：从在线用户列表中删除该用户
            pthread_mutex_lock(&mutex);
            for (int i = 0; i < user_count; i++) {
                // 找到当前用户的位置
                if (user_list[i].fd == cfd) {
                    // 后面的用户向前覆盖，删除当前用户
                    for (int j = i; j < user_count - 1; j++)
                        user_list[j] = user_list[j + 1];
                    user_count--;  // 在线用户数-1
                    break;
                }
            }
            pthread_mutex_unlock(&mutex);

            close(cfd);  // 关闭客户端连接
            break;       // 退出循环，线程自动结束
        }

        // ============== 正常接收消息，打印并回显 ==============
        std::cout << "📩 收到用户 " << user_id << " 的消息：" << buf << std::endl;
        // 回显消息：把消息发回客户端（测试用，后期改造成聊天转发）
        write(cfd, buf, n);
    }
    return NULL;
}

// ==================== 错误处理函数 ====================
void sys_err(const char *str) {
    perror(str);   // 打印系统错误信息
    exit(1);      // 退出程序
}
```

---

# 二、客户端代码 `client.cpp`（完整版详细注释）

cpp

运行

```
// ==================== 头文件说明 ====================
#include <iostream>     // C++ 输入输出
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <unistd.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>  // IP地址转换函数：inet_pton

// 服务器端口（必须和服务端完全一致）
#define SERV_PORT 9527

// ==================== 错误处理函数 ====================
void sys_err(const char *str) {
    perror(str);
    exit(1);
}

// ==================== 主函数（客户端入口） ====================
int main() {
    int cfd;                          // 客户端Socket：和服务器通信
    struct sockaddr_in serv_addr;     // 服务器地址结构体
    char buf[1024];                   // 收发消息的缓冲区
    int n;                            // 接收数据长度

    // 1. 创建客户端TCP Socket
    cfd = socket(AF_INET, SOCK_STREAM, 0);
    if (cfd == -1) sys_err("客户端socket创建失败");

    // 2. 配置服务器地址信息
    serv_addr.sin_family = AF_INET;         // IPv4协议
    serv_addr.sin_port = htons(SERV_PORT);   // 服务器端口9527
    // 服务器IP：127.0.0.1（本机测试专用）
    inet_pton(AF_INET, "127.0.0.1", &serv_addr.sin_addr);

    // 3. 连接服务器（客户端核心步骤）
    if (connect(cfd, (struct sockaddr *)&serv_addr, sizeof(serv_addr)) == -1)
        sys_err("连接服务器失败");

    // ==================== 核心功能：学号登录 ====================
    std::cout << "请输入你的学号：";
    // C++ 读取用户输入的学号
    std::cin >> buf;
    // 发送学号给服务器，完成登录
    write(cfd, buf, strlen(buf));
    std::cout << "✅ 学号 " << buf << " 登录成功！" << std::endl;

    // ==================== 循环收发消息 ====================
    while (1) {
        memset(buf, 0, sizeof(buf));
        // 提示用户输入消息
        std::cout << "请输入消息：";
        std::cin >> buf;

        // 发送消息到服务器
        write(cfd, buf, strlen(buf));
        // 输入 quit 退出客户端
        if (strcmp(buf, "quit") == 0) break;

        // 接收服务器回显的消息
        memset(buf, 0, sizeof(buf));
        n = read(cfd, buf, sizeof(buf));
        // 服务器断开连接则退出
        if (n <= 0) break;

        // 打印服务器返回的消息
        std::cout << "📢 服务器回显：" << buf << std::endl;
    }

    // 4. 关闭客户端连接
    close(cfd);
    return 0;
}
```
