---
data: 2026-02-24
---
- 思路和解法同[[排列树-递归交换法实现]]：
![[Pasted image 20260224193921.png]]
- 重点是理解:
```cpp
		//以当前节点开始，把剩余元素的孩子节点生成
		for (int k = i; k < length; ++k)
		{
			if (number >= arr[k])
			{
				vec.push_back(arr[k]);
				func(k + 1, number - arr[k]);  //不允许一个元素重复选
				//func(k, number - arr[k]); //允许一个元素重复选
				vec.pop_back();
			}
		}
```


### 代码实现
```cpp
#include<iostream>
using namespace std;
#include<vector>
int arr[] = { 4,8,12,16,7,9,3};
const int length = sizeof(arr) / sizeof(arr[0]);
int number = 18;
vector<int>vec;//存放选择的数字

void func(int i,int number)
{
	if (number == 0)
	{
		for (int v : vec)
		{
			cout << v << " ";
		}
		cout << endl;
	}
	else
	{
		//以当前节点开始，把剩余元素的孩子节点生成
		for (int k = i; k < length; ++k)
		{
			if (number >= arr[k])
			{
				vec.push_back(arr[k]);
				func(k + 1, number - arr[k]);  //不允许一个元素重复选
				//func(k, number - arr[k]); //允许一个元素重复选
				vec.pop_back();
			}
		}
	}
}
int main()
{
	func(0, number);
	return 0;
}
```