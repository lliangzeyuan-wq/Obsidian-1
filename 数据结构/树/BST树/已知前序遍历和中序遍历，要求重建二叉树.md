---
data: 2026-02-02
---
[8判断二叉树是否是平衡树_ev_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1FT421k7LL?spm_id_from=333.788.videopod.episodes&vd_source=43c9de78f6e5f2b05790188e274ad943&p=116)

![[Pasted image 20260202194343.png]]

  ![[Pasted image 20260202195227.png]]

### 问题描述
已经知道一颗BSTree的前序遍历和中序遍历，但不知道BSTree，要重建BSTree
### 代码
```cpp
public:
	//重建二叉树(已知前序遍历和中序遍历的结果)
	void rebuild(int pre[], int i, int j, int in[], int m, int n) {
		root_ = _rebuild(pre, i, j, in, m, n);
	}
private:
	//重建二叉树递归实现(已知前序遍历和中序遍历的结果)
	Node* _rebuild(int pre[], int i, int j, int in[], int m, int n) {
		if (i > j || m > n) {
			return NULL;
		}
		
		//创建当前子树的根节点
		Node* node = new Node(pre[i]);//拿前序的第一个数字创建子树根节点
		for (int k = m; k <= n; ++k) {
			if (in[k] == pre[i]) {
				node->left_ = _rebuild(pre, i + 1, i + (k - m), in, m, k - 1);
				node->right_ = _rebuild(pre, i + (k - m) + 1, j, in, k + 1, n);
				return node;
			}
		}
		return node;
	}
```