---
data: 2026-02-24
---
[3红黑树左旋转和右旋转代码讲解_ev_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1FT421k7LL?spm_id_from=333.788.videopod.episodes&vd_source=43c9de78f6e5f2b05790188e274ad943&p=125)

### AVL树的左旋转代码
```cpp
```cpp
private:
	//左旋转操作 以参数node为轴做左 旋转操作，并把新的根节点返回
	Node* leftRotate(Node* node)
	{
		Node* child = node->left_;
		node->right_ = child->left_;
		child->left_ = node;
		//高度更新
		node->height_ = max(height(node->left_), height(node->right_)) + 1;
		child->height_ = max(height(child->left_), height(node->right_)) + 1;
		//返回旋转后的子树新的根节点
		return child;
	}
```


- **红黑树的旋转和AVL树的最大的不同在于引入了parent_，即指向父节点的指针**
### 红黑树的左旋转
- 说实话，有点难，结合视频食用
```cpp
private:
	//左旋转
	void leftRotate(Node* node) {
		Node* child = node->right_;
		child->parent_ = node->parent_;
		if (node->parent_ == nullptr) {
			//node本身就是root节点
			root_ = child;
		}
		else {
			if (node->parent_->left_ == node) {
				//node在父节点的左孩子
				node->parent_->left_ = child;
			}
			else {
				//node在父节点的右孩子
				node->parent_->right_ = child;
			}
		}
		node->right_ = child->left_;
		if (child->left_ != nullptr) {
			child->left_->parent_ = node;
		}
		child->left_ = node;
		node->parent_ = child;
	}
```

### 红黑树的右旋转
```cpp
private:

```