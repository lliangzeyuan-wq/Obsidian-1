---
data: 2026-02-08
---
### 理论
#### 1.空树：插入黑色
#### 2.非空：插入红色
###### 情况1：
- 父亲和叔叔节点都为红色
![[Pasted image 20260208143441.png]]
###### 情况2
- 父亲为红色，叔叔为黑色，并且A、B、C在同一侧
![[Pasted image 20260208143555.png]]
###### 情况3
- 父亲为红色，叔叔为黑色，并且A、B、C不在同一侧
![[Pasted image 20260208143758.png]]


### 代码
```cpp
public:
	//插入操作
	void insert(const T& val) {
		if (root_ == NULL) {
			root_ = new Node(val);//节点默认是黑色的
			return;
		}
		Node* parent = nullptr;
		Node* cur = root_;
		while (cur != nullptr) {
			if (cur->data_ > val) {
				parent = cur;
				cur = cur->left_;
			}
			else if (cur->data_ < val) {
				parent = cur;
				cur = cur->right_;
			}
			else {
				return;//节点已经有了，不用插入
			}
		}
		
		Node* node = new Node(val, parent, nullptr, nullptr, RED);
		if (parent->data_ > val) {
			parent->left_ = node;
		}
		else {
			parent->right_ = node;
		}

		//如果新插入的红色节点，父节点也是红色，不满足红黑树性质，进行插入调整操作
		if (RED == color(parent)) {
			fixAfterInsert(node);
		}
	}
private:
	//红黑树的插入调整操作
	void fixAfterInsert(Node* node) {
		//如果当前红色节点的父节点也是红色，继续调整（前提：node为红色节点)
		while (color(parent(node)) == RED) {
			//插入的节点在左子树当中
			if (left(parent(parent(node))) == parent(node)) {
				Node* uncle = right(parent(parent(node)));
				//情况1
				if (RED == color(uncle)) {
					setColor(parent(node), BLACK);
					setColor(right(parent(parent(node))), BLACK);
					setColor(parent(parent(node)), RED);
					node = parent(parent(node));
				}
				else {
					//先处理情况3的前半部分，将他转化成情况2
					if (right(parent(node)) == node) {
						//你旋转下去的节点才是之后处理的node,也就是旋转之前的parent(node)
						node = parent(node);
						leftRotate(node);
					}
					//统一处理情况2
					setColor(parent(node), BLACK);
					setColor(parent(parent(node)), RED);
					rightRotate(parent(parent(node)));
					break;//调整已经完成（看图，因为调整完之后顶上为黑色）
				}
			}
			//插入的节点在右子树中
			else {
				Node* uncle = left(parent(parent(node)));
				//插入的节点在右子树当中
				if (RED == color(uncle)) {
					setColor(parent(node), BLACK);
					setColor(parent(parent(node)), RED);
					setColor(left(parent(parent(node))), BLACK);
					node = parent(parent(node));
				}
				else {
					//先处理情况3
					if (left(parent(node)) == node) {
						node = parent(node);
						rightRotate(node);
					}
					//再统一处理情况2，3
					setColor(parent(node), BLACK);
					setColor(parent(parent(node)), RED);
					leftRotate(parent(parent(node)));
					break;
				}
			}
		}
		//此处强制root为黑色节点（红色节点变为黑色节点没有任何其他的影响）
		setColor(root_, BLACK);

	}
```