
[2Dijkstra迪杰斯特拉算法原理讲解_ev_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1Zy411q7Lq?spm_id_from=333.788.player.switch&vd_source=43c9de78f6e5f2b05790188e274ad943&p=15)

## 1. 定义概览

Dijkstra (迪杰斯特拉) 算法是典型的**单源最短路径算法**，用于计算一个节点到==其他所有节点==的最短路径。主要特点是以起始点为中心向外层层扩展，直到扩展到终点为止。Dijkstra 算法是很有代表性的最短路径算法。 （主要用了==贪心==的思想）

---

## 2. 算法描述
  
### 1) 算法思想

设 \(G=(V,E)\) 是一个带权有向图，把图中顶点集合 V 分成两组：

- **集合 S**：已求出最短路径的顶点集合
    
    - 初始时 S 中只有一个源点
    - 以后每求得一条最短路径，就将对应顶点加入集合 S
    - 直到全部顶点都加入到 S 中，算法结束
    
- **集合 U**：其余未确定最短路径的顶点集合
    
    - 按最短路径长度的递增次序依次把 U 中的顶点加入 S 中
    - 加入过程中，总保持从源点 v 到 S 中各顶点的最短路径长度，不大于从源点 v 到 U 中任何顶点的最短路径长度
    

此外，每个顶点对应一个距离：

- S 中的顶点的距离：从 v 到此顶点的最短路径长度
- U 中的顶点的距离：从 v 到此顶点，只包括 S 中的顶点为中间顶点的当前最短路径长度

### 2) 算法步骤
- 每一次从U集合中选取权值最小的顶点，放入S集合
- 加入S集合后，需要更新U集合中剩余顶点的权值信息
- 计算A到其他所有节点的最小值
![[Pasted image 20260525163445.png]]
- 第一步 （INF 表示不可达/无穷大） ：计算A直接到其他所有节点的距离
![[Pasted image 20260525163541.png]]
- 第二步 ：选择最小的C放入S集合中，然后根据 `dis[i] > min + graph[k][i]` ,   就是看现在U集合中的值和