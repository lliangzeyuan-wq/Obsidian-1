### 问题核心

你的后端**在虚拟机本地（127.0.0.1:8080）能正常访问**，但**外部 IP（192.168.198.131:8080）访问被拒绝**，这是典型的**虚拟机防火墙拦截端口**或**网络配置问题**，和代码本身无关。

---

### ✅ 分步解决（按顺序执行）

#### 1. 验证后端监听地址（先确认代码配置是否生效）

在虚拟机终端执行：

bash

运行

```
ss -tuln | grep 8080
```

✅ 预期结果：看到 `0.0.0.0:8080`（表示监听所有网卡，包括你的虚拟机 IP）

❌ 如果只看到 `127.0.0.1:8080`：说明监听配置异常（虽然代码是 `INADDR_ANY`，但可能系统层面限制了）

---

#### 2. 开放防火墙 8080 端口（最可能的原因）

你的虚拟机是 Ubuntu 系统，默认防火墙会拦截外部对 8080 端口的访问：

bash

运行

```
# 开放8080端口（TCP协议）
sudo ufw allow 8080/tcp

# 查看防火墙状态，确认规则生效
sudo ufw status
```

✅ 确认输出中包含：`8080/tcp ALLOW Anywhere`

---

#### 3. 再次测试连通性

在虚拟机终端重新测试：

bash

运行

```
curl http://192.168.198.131:8080
```

✅ 成功标志：返回 `{"success":true,"message":"请求成功"}`

❌ 如果还是拒绝：检查虚拟机网络模式（下一步）

---

#### 4. 检查虚拟机网络模式（如果是 NAT 模式）

如果你的虚拟机是 **NAT 模式**（默认常见），需要额外配置**端口转发**：

- 打开虚拟机软件（VMware/VirtualBox）→ 虚拟机设置 → 网络 → 高级 → 端口转发
- 添加规则：
    
    - 主机端口：`8080`
    - 虚拟机 IP：`192.168.198.131`
    - 虚拟机端口：`8080`
    
- 保存后，宿主机访问 `127.0.0.1:8080` 就会自动转发到虚拟机后端

---

### 🧪 最终验证

完成以上步骤后：

1. 虚拟机内：`curl http://192.168.198.131:8080` → 成功
2. 宿主机浏览器：`http://192.168.198.131:8080` → 成功访问后端接口

---

### 💡 补充：为什么 127.0.0.1 能访问？

- `127.0.0.1` 是**虚拟机本地回环地址**，防火墙默认不拦截本地访问
- `192.168.198.131` 是**虚拟机对外网卡 IP**，防火墙默认拦截外部对非标准端口（如 8080）的访问

现在按步骤操作，就能解决「连接被拒绝」的问题了！需要我帮你一步步核对防火墙配置吗？