### 问题根源：**后端服务没在运行 / 监听地址不对**

你现在在虚拟机里连自己的新 IP 都连不上，说明问题出在**后端服务本身**，而不是网络。

---

### ✅ 分步排查（按顺序执行）

#### 步骤 1：确认后端服务是否在运行

在虚拟机终端执行：

bash

运行

```
ps aux | grep "./server"
```

- **如果看不到 `./server` 进程**：说明后端服务已经停了，需要重新启动：
    
    bash
    
    运行
    
    ```
    cd ~/campus_social
    ./server
    ```
    
    看到 `✅ 服务器启动成功！端口：8080` 才算启动成功。

#### 步骤 2：确认端口监听状态

启动服务后，再执行：

bash

运行

```
ss -tuln | grep 8080
```

✅ **正确结果**：

plaintext

```
tcp  LISTEN  0  10  0.0.0.0:8080  0.0.0.0:*
```

- 如果看到 `127.0.0.1:8080`：说明后端只监听了本地回环地址，外部（包括你这个新 IP）无法访问，需要检查 `server.cpp` 里的监听配置（确保是 `INADDR_ANY`），然后重新编译启动。

#### 步骤 3：先测试本地访问

在虚拟机终端先测试**本地回环地址**：

bash

运行

```
curl http://127.0.0.1:8080
```

- ✅ 成功：返回 `{"success":true,"message":"请求成功"}`
- ❌ 失败：说明后端服务本身有问题（比如代码报错、端口被占用）

#### 步骤 4：再测试新 IP 访问

本地通了之后，再测试你的新 IP：

bash

运行

```
curl http://10.186.49.122:8080
```

这时候应该就能通了！

---

### 🧪 最终验证

等虚拟机里 `curl http://10.186.49.122:8080` 成功后，再去宿主机浏览器访问：

text

```
http://10.186.49.122:8080
```

就能看到后端响应，注册功能也不会再报 `Failed to fetch` 了！

需要我帮你检查 `server.cpp` 里的监听配置是否正确吗？