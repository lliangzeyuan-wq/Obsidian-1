### 核心问题定位

你这个场景的本质是：**编辑资料页在提交「完成」时，没有正确携带登录后的 Token**，导致后端认为你未登录，所以返回「请先登录」提示，无法完成更新和跳转。

---

### 🔍 一步步排查原因（按顺序查）

#### 1. 先确认 Token 是否真的存在（最基础验证）

打开浏览器按 `F12` → 切换到「**应用 (Application)**」面板 → 找到「**本地存储 (Local Storage)**」：

- 看里面有没有 `token` 字段，值是不是登录后后端返回的字符串（比如 `token_1712345678`）
    
    - ✅ 如果有：说明登录时 Token 存对了，问题出在「编辑页读取 / 发送 Token」环节
    - ❌ 如果没有：说明登录成功后，前端**根本没把 Token 存进去**，这是登录逻辑的 bug
    

---

#### 2. 检查编辑页的「完成」按钮逻辑

找到编辑资料页（比如 `profile-edit.html`）里的「完成」按钮点击事件，核心看 2 点：

1. **有没有读取 Token**：
    
    javascript
    
    运行
    
    ```
    // 正确写法：先从 localStorage 拿 Token
    const token = localStorage.getItem('token');
    if (!token) {
      alert("请先登录");
      return; // 没 Token 就直接阻止请求
    }
    ```
    
2. **有没有通过 `api.js` 发请求**：
    
    - 必须调用 `api.updateProfile(数据)`，而不是自己写 `fetch`
    - 因为你之前的 `api.js` 里，`request` 方法会**自动把 Token 放到 Authorization 头里**，自己写 `fetch` 容易漏加 Header
    

---

#### 3. 检查「页面路径是否一致」（容易被忽略的坑）

你现在是用 `Open in default browser` 打开的，地址是 `file:///C:/Users/.../xxx.html`：

- ✅ 要求：**登录页、我的页、编辑页必须都在 `file://` 协议下打开**，这样 `localStorage` 才会共享
- ❌ 风险：如果中途切换到 `http://127.0.0.1:5500`（Live Server），浏览器会认为是不同「站点」，`localStorage` 被隔离，编辑页读不到 Token

---

#### 4. 最后检查后端 Token 验证逻辑

看 `server.cpp` 里的 `verifyToken` 函数：

cpp

运行

```
string verifyToken(MYSQL* conn, const string& token) {
    if (token.empty()) return ""; // 空 Token 直接返回未登录
    // ... 数据库查询逻辑 ...
}
```

- 如果编辑页传了空 Token，`verifyToken` 会返回空，后端就会让前端「请先登录」
- 所以根源还是**前端没把 Token 传给后端**，不是后端的问题

---

### ✅ 最快修复方案（直接改代码）

在编辑页的「完成」按钮点击事件里，**强制加 Token 校验**，示例代码：

javascript

运行

```
// 假设这是编辑页的「完成」按钮点击事件
document.getElementById('complete-btn').addEventListener('click', async function() {
    // 1. 先拿 Token，没有就提示登录
    const token = localStorage.getItem('token');
    if (!token) {
        alert("请先登录！");
        window.location.href = "index.html"; // 跳回登录页
        return;
    }

    // 2. 收集你填的资料数据
    const formData = {
        id: "当前用户ID", // 可以从我的页传过来，或者从 Token 解析
        gender: document.querySelector('input[name="gender"]:checked').value,
        // ...其他你填的字段（年级、专业、兴趣标签等）
    };

    // 3. 调用 api 更新资料（必须用 api.js 的方法）
    try {
        const res = await api.updateProfile(formData);
        if (res.success) {
            alert("资料更新成功！");
            window.location.href = "profile.html"; // 跳回「我的」页面
        } else {
            alert(res.message); // 比如「请先登录」
        }
    } catch (err) {
        console.error("更新失败：", err);
    }
});
```

---

### 🚀 总结

你现在的 bug 不是「登录状态丢失」，而是**编辑页提交时没带 Token**，后端不认这个请求。只要保证：

1. 登录后 Token 成功存在 `localStorage`
2. 编辑页提交时，通过 `api.updateProfile()` 发请求（自动带 Token）
3. 所有页面都在同一种协议下打开（要么都 `file://`，要么都 `http://`）

点「完成」后就会正常跳回「我的」页面，显示你刚填的资料啦！

需要我帮你把编辑页的完整「完成」按钮代码写出来吗？