### 关键结论：**后端请求成功了，但没返回用户资料的 `data` 数据！**

从 Console 输出能看到：

json

```
{message: '请求成功', success: true}
```

只有成功提示，**没有 `data` 字段**，所以前端根本拿不到你填的资料，自然显示为 `--`。

---

### 🎯 问题根因 & 解决步骤

#### 1. 后端查询接口没返回数据（核心问题）

你的 `/api/profile/{userId}` 接口，现在只返回了 `success: true`，**没有把查询到的用户资料放到 `data` 里**，比如应该返回：

json

```
{
  "success": true,
  "data": {
    "name": "梁泽远",
    "gender": "male",
    "grade": "大一",
    "major": "软件工程",
    // ... 其他你填的字段
  }
}
```

但现在后端只给了成功状态，没给数据。

#### 2. 先确认「保存是否真的写入数据库」

你之前「保存成功」，但要确认：

- 保存接口（`update`）是不是把数据写到了 **`user_profile` 表**（和查询接口查的表一致）
- 去数据库执行：
    
    sql
    
    ```
    SELECT * FROM user_profile WHERE user_id = '2025300446';
    ```
    
    如果查不到记录 → 保存接口其实没写入数据，需要修保存逻辑。

#### 3. 修改后端查询接口（把数据带回来）

打开后端处理 `/api/profile/{userId}` 的代码，确保：

- 用 `user_id` 查询到用户资料
- 把查询结果放到 `data` 字段里返回，比如（伪代码）：
    
    cpp
    
    运行
    
    ```
    // 假设查到了 profile 数据
    Json::Value result;
    result["success"] = true;
    result["message"] = "请求成功";
    result["data"]["name"] = profile.name;
    result["data"]["gender"] = profile.gender;
    result["data"]["grade"] = profile.grade;
    result["data"]["major"] = profile.major;
    // ... 其他字段
    ```
    

---

### ✅ 修复后效果

后端返回带 `data` 的数据后，前端 `profile.html` 里的渲染代码就能拿到数据，把 `--` 替换成你填的内容啦！

需要我帮你看看后端查询接口的代码吗？我可以告诉你怎么把数据放到 `data` 里返回～