### 心结论：**跨域配置仍未生效**，问题出在 **后端代码未正确更新 / 编译** 或 **浏览器缓存未清空**

从你的 Network 面板看，`register` 请求依然是 `CORS error`，说明浏览器还是没拿到正确的跨域响应头。

---

### ✅ 按顺序执行以下 4 步，100% 解决问题

#### 步骤 1：确认后端代码是「最终修复版」

请确保你的 `server.cpp` 里，**跨域头部分是动态获取 Origin 的版本**（我之前发给你的最终版）：

cpp

运行

```
// 🔥 关键代码：动态获取请求 Origin，兼容 credentials: 'include'
size_t origin_pos = request.find("Origin: ");
string origin = "*";
if (origin_pos != string::npos) {
    origin = request.substr(origin_pos + 8);
    size_t end_pos = origin.find("\r\n");
    if (end_pos != string::npos) origin = origin.substr(0, end_pos);
}

string resp_headers =
    "HTTP/1.1 200 OK\r\n"
    "Content-Type: application/json; charset=utf-8\r\n"
    "Access-Control-Allow-Origin: " + origin + "\r\n"
    "Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS\r\n"
    "Access-Control-Allow-Headers: Content-Type, Authorization\r\n"
    "Access-Control-Allow-Credentials: true\r\n"
    "Connection: close\r\n\r\n";
```

#### 步骤 2：彻底重新编译后端

在虚拟机终端执行：

bash

运行

```
# 1. 停止旧的后端进程（如果还在运行）
pkill -f "./server"

# 2. 清理旧编译产物
make clean

# 3. 重新编译（用你的 Makefile）
make

# 4. 启动新后端
./server
```

看到 `✅ 服务器启动成功！端口：8080` 才算启动成功。

#### 步骤 3：在虚拟机内验证跨域是否生效（关键测试）

在虚拟机终端执行以下 2 条命令，**测试后端是否真的返回了正确跨域头**：

1. 测试 OPTIONS 预检请求：
    
    bash
    
    运行
    
    ```
    curl -v -X OPTIONS http://10.186.49.122:8080/api/register \
      -H "Origin: http://127.0.0.1:5500"
    ```
    
    ✅ 成功标志：返回的头里包含：
    
    plaintext
    
    ```
    Access-Control-Allow-Origin: http://127.0.0.1:5500
    Access-Control-Allow-Credentials: true
    ```
    
2. 测试 POST 注册请求：
    
    bash
    
    运行
    
    ```
    curl -v -X POST http://10.186.49.122:8080/api/register \
      -H "Origin: http://127.0.0.1:5500" \
      -H "Content-Type: application/json" \
      -d '{"studentId":"1112","name":"1112","gender":"male","password":"123456"}'
    ```
    
    ✅ 成功标志：返回 `{"success":true,"message":"注册成功"}`
    

#### 步骤 4：浏览器强制刷新，清除缓存

在浏览器按 **`Ctrl + Shift + R`**（Windows）或 **`Cmd + Shift + R`**（Mac），**彻底清空缓存并刷新页面**，然后重新测试注册功能。

---

### 💡 如果还是失败？最后排查点

1. **后端没跑起来**：确认虚拟机终端里 `./server` 正在运行，没有报错。
2. **IP 写错**：确认 `api.js` 里的 `baseUrl` 是 `http://10.186.49.122:8080`，没有拼写错误。
3. **浏览器缓存顽固**：可以尝试「右键刷新 → 清空缓存并硬性重新加载」，或直接打开「无痕模式」测试。

---

### 🎯 最终预期

完成以上步骤后，浏览器里的 `CORS error` 会消失，注册请求会成功，你会看到 **「✅ 注册成功！请登录」** 的弹窗！

需要我帮你逐行核对 `server.cpp` 的跨域代码吗？