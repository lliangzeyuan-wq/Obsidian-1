### 从终端截图确认：**后端服务已经启动了**（PID 5127 的 `./server` 进程正在运行）

请求还卡在 `pending`，说明问题出在**网络连通性 / 监听配置 / 接口逻辑**上，按下面步骤排查：

---

### 1. 先确认后端监听地址（最关键！）

后端代码里如果监听的是 `127.0.0.1:8080`，**只有虚拟机内部能访问**，Windows 前端根本连不上！

- 打开后端代码，找到 `socket` 绑定部分，看是否是：
    
    c
    
    运行
    
    ```
    // ✅ 允许外部访问（正确）
    struct sockaddr_in serv_addr;
    serv_addr.sin_addr.s_addr = INADDR_ANY; // 监听 0.0.0.0
    serv_addr.sin_port = htons(8080);
    ```
    
- 如果是 `serv_addr.sin_addr.s_addr = inet_addr("127.0.0.1");` → **错误**，改成 `INADDR_ANY` 后重启后端。

---

### 2. 开放虚拟机 8080 端口（防火墙拦截）

在虚拟机终端执行：

bash

运行

```
sudo ufw allow 8080/tcp
sudo ufw reload
```

让 Windows 能访问虚拟机的 8080 端口。

---

### 3. 验证网络连通性（Windows → 虚拟机）

在 Windows 浏览器里输入：

plaintext

```
http://10.186.49.122:8080
```

- ✅ 能打开后端页面 → 网络通
- ❌ 打不开 → 回到第 1/2 步，检查监听地址和防火墙

---

### 4. 修正前端请求地址（核心！）

你的前端跑在 Windows（`127.0.0.1:5501`），`localhost` 指的是 Windows 自己，不是虚拟机！

打开 `profile-edit.html`，把 `fetch` 地址改成**虚拟机 IP**：

javascript

运行

```
const response = await fetch('http://10.186.49.122:8080/api/profile/update', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer ' + token
  },
  body: JSON.stringify(profile)
});
```

保存后刷新页面，重新提交。

---

### 5. 最后看后端接口逻辑（是否阻塞）

如果上面都对，还是 pending → **后端接口代码阻塞了**：

- 看后端处理 `/api/profile/update` 的函数，有没有：
    
    - 死循环
    - 长时间数据库查询
    - 未捕获的异常导致进程卡住
    
- 修复后重启后端服务，再提交。

---

### ✅ 按顺序做完这 5 步，请求一定能正常响应！

需要我帮你检查后端代码的监听配置吗？直接贴出 `socket` 绑定部分就行～