
# 最大公约数
```cpp
ll gcd(ll a,ll b)
{
	a=abs(a);
	b=abs(b);
	while(b!=0)
	{
		ll temp=b;
		b=a%b;
		a=temp;
	}
	return a;
}
```
- 0和x的最大公约数是x
- gcd(0,x)=gcd(x,0)=x;


# 最小公倍数
![[Pasted image 20260410150913.png]]

```cpp
int lcm(int a,int b)
{
	return (a*b)/gcd(a,b);
}
```