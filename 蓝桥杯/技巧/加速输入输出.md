---
data: 2026-02-27
---
- 这两行直接粘在main函数的后面

```cpp
int main()
{
	//这两行直接粘在main函数开头
	ios::sync_with_stdio(false);
	cin.tie(nullptr);


return 0;
}
```

 - 加了这两行后，**不要混用 cin/cout 和 scanf/printf**！
 - 作用：提高cin/cout的输入输出的效率
