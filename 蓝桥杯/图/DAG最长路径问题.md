---
data: 2026-03-26
---
## 1. 通俗描述

我们有一张**任务依赖图**（==有向无环图==）：

- 每个数字（1/2/4/5/6）代表**一个任务**
- 箭头 `→` 代表**依赖关系**（必须先完成前面的任务，才能做后面的）
- 整张图**没有循环**（不会出现你依赖我、我依赖你的死循环）

我们要解决的问题：

**在所有合法的任务执行顺序中，找到能完成「最多任务」的那条路径**

（也就是：从开始到结束，走一步算一个任务，最多能走多少步 / 完成多少个任务）


==**即：路径最长的那一条**==