## fg 命令（foreground）

### 作用

把**后台运行**或**暂停**的作业，切回**前台**，重新接管终端。

### 语法

bash

运行

```
fg          # 默认：最近一个任务（带 + 号的）
fg %1       # 作业号 1
fg %vim     # 名字匹配
```

### 例子

接上面，后台跑着 `long_run.sh`，想切回来：

bash

运行

```
fg
```

终端又被它占了，输出继续打在屏幕上，你可以 `Ctrl+C` 终止它。