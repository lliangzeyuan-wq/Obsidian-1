# 打开文件
```c
#include<sys/types.h>
#include<sys/stat.h>
#include<fcntl.h>

int open(const char *path,int flag);
//成功时返回文件描述符，失败时返回-1
//path：文件名的字符串地址
//flag：文件的打开模式
```
> 文件的打开模式
> O_CREAT   必要时船舰文件
> O_TRUNC  删除全部现有数据
> O_APPEND 维持现有数据，保存到其后面
> O_RDONLY  只读打开
> O_WRONLY  只写打开
> O_RDWR  
