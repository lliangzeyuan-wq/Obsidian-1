
### 知识点讲解

Vue 内置专门用于动态切换组件的标签是 **`<component>`**，搭配 `:is` 动态绑定要渲染的组件名称 / 对象，完整标准写法：

html

预览

```
<component :is="变量名"></component>
```



`is` 是 Vue 给内置标签 `<component>` 专属的**属性**；

前面加冒号 `:is` = `v-bind:is`，代表**动态绑定**，里面写的是 JS 变量，不是静态字符串。