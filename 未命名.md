cpp

运行

```cpp
// 万能头文件，包含C++所有常用标准库，竞赛/刷题场景直接用
#include <bits/stdc++.h>
using namespace std;

// 定义常量N，作为线段树的数值范围上限
// 注意：此写法仅适用于a[i]数值范围较小（≤1e5）的场景
// 若a[i]范围到1e9，必须先做离散化，将数值映射到1~n的连续区间
const int N = 1e5 + 9;

// 全局变量说明：
// n: 输入序列的长度
// a[N]: 存储输入的原始序列（1-based下标）
// dp[N]: dp[i] = 以a[i]为结尾的最长递增子序列（LIS）的长度
// t[N<<2]: 线段树数组，<<2等价于*4，线段树标准开4倍大小避免越界
// 【核心】线段树的下标 = 原数组的数值a[i]，节点值 = 该数值对应的最大dp值
int n, a[N], dp[N], t[N << 2];

/**
 * @brief 线段树单点更新函数
 * @param o 当前线段树节点编号（根节点固定为1）
 * @param l 当前节点管辖的区间左端点
 * @param r 当前节点管辖的区间右端点
 * @param pos 要更新的位置（对应原数组的数值a[i]）
 * @param val 要更新的值（对应当前a[i]的dp[i]）
 * 作用：将线段树中pos位置的值，更新为「原值和val的较大者」，保证每个数值存的是最大dp值
 */
void updata(int o, int l, int r, int pos, int val)
{
    // 先更新当前节点的最大值：始终存当前区间的最大dp值
    // 保证线段树每个节点永远维护其管辖区间内的最大dp值
    t[o] = max(t[o], val);
    // 递归终止条件：到达叶子节点（l==r，对应某个具体数值），更新完成，直接返回
    if (l == r) return;

    // 计算当前区间中点，拆分左右子树（>>1等价于/2，位运算效率更高）
    int mid = (l + r) >> 1;
    // 要更新的位置在左子树，递归更新左子节点（编号2*o，管辖区间[l, mid]）
    if (pos <= mid) updata(2 * o, l, mid, pos, val);
    // 要更新的位置在右子树，递归更新右子节点（编号2*o+1，管辖区间[mid+1, r]）
    else updata(2 * o + 1, mid + 1, r, pos, val);
}

/**
 * @brief 线段树区间最大值查询函数
 * @param o 当前线段树节点编号（根节点固定为1）
 * @param l 当前节点管辖的区间左端点
 * @param r 当前节点管辖的区间右端点
 * @param nl 要查询的目标区间左端点
 * @param nr 要查询的目标区间右端点
 * @return 目标区间[nl, nr]内的最大dp值
 * 作用：快速查询「数值在[nl, nr]范围内」的最大dp值，用于计算当前a[i]的dp[i]
 */
int getmax(int o, int l, int r, int nl, int nr)
{
    // 递归终止条件：当前节点的区间完全被目标区间包含，直接返回当前节点存的最大值
    if (l == nl && r == nr) return t[o];

    // 计算当前区间中点
    int mid = (l + r) >> 1;
    // 情况1：目标区间完全在左子树，只递归查询左子节点
    if (mid >= nr) return getmax(2 * o, l, mid, nl, nr);
    // 情况2：目标区间完全在右子树，只递归查询右子节点
    else if (mid < nl) return getmax(2 * o + 1, mid + 1, r, nl, nr);
    // 情况3：目标区间跨左右子树，分别查询左右子树的最大值，再取整体最大值
    else return max(getmax(2 * o, l, mid, nl, mid), getmax(2 * o + 1, mid + 1, r, mid + 1, nr));
}

int main()
{
    // 关闭cin/cout同步流，解除和stdio的绑定，大幅加速输入输出
    // 避免大数据量下cin/cout超时，竞赛必加
    ios::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);

    // 读入序列长度n
    cin >> n;
    // 读入原始序列，1-based下标，和线段树下标规则统一
    for (int i = 1; i <= n; i++) cin >> a[i];

    // ans存储最终答案：整个序列的最长递增子序列长度
    int ans = 0;

    // 【核心逻辑】遍历每个元素，计算dp[i]并更新线段树
    // 核心思路：
    // 1. 对每个a[i]，查询「数值1~a[i]」的最大dp值，+1得到dp[i]（在最长子序列末尾加当前元素）
    // 2. 用dp[i]更新线段树中a[i]位置的最大值，供后续元素查询
    // 3. 顺序必须是【先查后更】，避免把当前元素的dp[i]算进自己的查询里
    for (int i = 1; i <= n; i++)
    {
        // 【关键】查询[1,a[i]]的最大dp值，+1得到dp[i]
        // 注意：当前写法是【非递减】LIS（允许相等元素）
        // 若要实现【严格递增】LIS，把查询区间改为[1, a[i]-1]即可
        dp[i] = getmax(1, 1, N, 1, a[i]) + 1;

        // 用刚算出的dp[i]，更新线段树中a[i]位置的最大值
        // 保证线段树中a[i]位置，永远存「以a[i]结尾的最大dp值」
        updata(1, 1, N, a[i], dp[i]);

        // 更新全局最大值，最终ans就是整个序列的LIS长度
        ans = max(ans, dp[i]);
    }

    // 输出最终结果
    cout << ans << '\n';
    return 0;
}
```

---

### 补充关键说明

1. **时间复杂度**：每个元素执行 1 次查询 + 1 次更新，单次操作都是`O(logN)`，总复杂度 **`O(n logN)`**，完美解决朴素 LIS 的`O(n²)`超时问题。