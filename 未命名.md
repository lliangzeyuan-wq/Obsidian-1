=下面用一个完整、可直接运行的表单示例，把表格里所有属性都用上，帮你彻底理解它们的用法👇

---

## 完整代码示例

html

预览

```
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>表单属性示例</title>
    <style>
        /* 用 id 来设置表单样式，演示 id 的作用 */
        #myForm {
            width: 400px;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
        }
    </style>
</head>
<body>

<h2>在线订餐表单</h2>

<!-- 这里把所有属性都写进了 FORM 标签 -->
<form 
    name="OrderFoodOnline" 
    id="myForm"
    method="post" 
    action="process.html"
    autocomplete="on"
    novalidate
    target="_blank"
>
    <!-- 表单内容 -->
    <label>姓名：</label>
    <input type="text" name="username" placeholder="请输入姓名" required>
    <br><br>

    <label>手机号：</label>
    <input type="tel" name="phone" placeholder="请输入手机号" required>
    <br><br>

    <label>备注：</label>
    <textarea name="remark" rows="3"></textarea>
    <br><br>

    <button type="submit">提交订单</button>
</form>

</body>
</html>
```

---

## 逐个属性讲解（结合代码）

### 1. `name="OrderFoodOnline"`

- **作用**：给表单起一个唯一的名字，主要供 JavaScript 脚本使用。
- **用法示例**：
    
    js
    
    ```
    // 用 name 获取表单元素
    const form = document.forms['OrderFoodOnline'];
    console.log(form);
    ```
    

### 2. `id="myForm"`

- **作用**：表单的唯一标识符，主要用于 CSS 样式和 JavaScript 操作。
- **代码里的体现**：
    
    - CSS 里用 `#myForm` 设置了表单的宽度、边框和内边距。
    - JS 里可以用 `document.getElementById('myForm')` 获取这个表单。
    

### 3. `action="process.html"`

- **作用**：表单数据提交的目标地址。
- **说明**：用户点击提交后，浏览器会把表单数据发送到 `process.html` 这个页面（实际开发中一般是后端接口，比如 `/api/submit-order`）。

### 4. `method="post"`

- **作用**：指定表单数据的提交方式。
    
    - `get`：数据会拼在 URL 后面（比如 `process.html?username=张三&phone=138...`），长度有限制，不适合敏感数据。
    - `post`：数据放在请求体里发送，不会显示在 URL 上，安全且无长度限制，适合提交表单。
    
- **这里的用法**：用 `post` 提交订单数据，更安全。

### 5. `autocomplete="on"`

- **作用**：控制浏览器的自动填充功能。
    
    - `on`：开启自动填充（比如浏览器会自动帮用户填之前提交过的姓名、手机号）。
    - `off`：关闭自动填充，强制用户手动输入。
    
- **这里的用法**：开启自动填充，方便用户快速填写信息。

### 6. `novalidate`

- **作用**：禁用浏览器默认的表单验证。
- **说明**：正常情况下，`required` 的输入框为空时，浏览器会弹出提示阻止提交；加了 `novalidate` 后，浏览器就不会做这些检查，直接提交数据。
- **这里的用法**：演示禁用验证的效果，即使手机号没填，也能提交表单。

### 7. `target="_blank"`

- **作用**：指定表单提交后，响应页面在哪里打开。
    
    - `_blank`：在新窗口打开结果页面。
    - `_self`：在当前窗口打开（默认值）。
    - `_parent`：在父框架中打开。
    - `_top`：在整个窗口中打开。
    - `frame_name`：在指定的 iframe 中打开。
    
- **这里的用法**：提交订单后，结果页面会在新标签页打开，不影响当前页面。

---

## 运行效果说明

把上面的代码保存为 `.html` 文件，在浏览器里打开，你会看到：

1. 表单有了我们设置的样式（来自 `id="myForm"` 的 CSS）。
2. 浏览器会自动提示你之前填过的姓名、手机号（来自 `autocomplete="on"`）。
3. 即使不填必填项，也能提交表单（来自 `novalidate`）。
4. 提交后会在新标签页打开 `process.html`（来自 `target="_blank"`）。

需要我再给你写一个用 `get` 方式提交的版本，对比一下 URL 上的数据变化吗？

快速

编程

图像生成

更多