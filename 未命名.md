```cpp
#include <bits/stdc++.h>  // C++ 万能头文件，包含所有常用库
using namespace std;
#define int long long   // 防止权值溢出，用 long long

const int MAXN = 2005; // 最多 n 个叶子，总结点数 2n-1，开 2 倍空间足够

int n, cnt;             // n：叶子节点数，cnt：总节点编号
pair<int, int> e[MAXN]; // e[u] 存编号 u 的节点的左右孩子编号
string s[MAXN];         // 存每个叶子节点对应的字符/字符串
string chain[MAXN];     // 存每个叶子节点最终的哈夫曼编码
string cur = "";        // 递归时临时记录当前路径（0/1）

// 哈夫曼树节点结构体
struct node{
	int w;  // 权值
	int id; // 节点编号（叶子是1~n，非叶子从n+1开始）
    
	// 重载小于号，让优先队列变成【小顶堆】
	// 因为默认 priority_queue 是大顶堆，用 > 就能反过来变成小顶堆
	bool operator<(node x) const{
		return w > x.w;
	}
};

// 递归遍历哈夫曼树，生成编码（前序遍历）
// u：当前遍历到的节点编号
void search(int u){
	// 如果是【叶子节点】（原本的字符节点）
	if(u <= n){
		chain[u] = cur;  // 把当前路径存为该节点的编码
		return;
	}
    
	// 左孩子走 0
	cur += '0';
	search(e[u].first);   // 递归左子树
	cur.pop_back();       // 回溯，删掉最后一个 0
    
	// 右孩子走 1
	cur += '1';
	search(e[u].second);  // 递归右子树
	cur.pop_back();       // 回溯，删掉最后一个 1
}

signed main(){
	ios::sync_with_stdio(0);cin.tie(0);cout.tie(0); // 加速 cin/cout
	
	cin >> n;             // 输入字符个数
	cnt = n;              // 初始节点编号 = n
	
	priority_queue<node> q; // 小顶堆（权值最小优先）
    
	// 输入 n 个字符 + 权值，放入小根堆
	for(int i = 1, w; i <= n; ++ i){
		cin >> s[i] >> w;
		q.push({w, i});  // 权值w，编号i
	}
    
	// 特殊情况：只有一个字符，编码直接是 0
	if(n == 1){
		cout << s[1] << " 0";
		return 0;
	}
    
	// 经典哈夫曼树构建：每次取两个最小的合并
	while(q.size() > 1){
		// 取出权值最小的两个节点
		node x = q.top(); q.pop();
		node y = q.top(); q.pop();
        
		// 新建一个父节点，编号 cnt++
		e[++ cnt] = {x.id, y.id}; // 左孩子x，右孩子y
        
		// 父节点权值 = 两个子节点权值和，放回堆
		q.push({x.w + y.w, cnt});
	}
    
	// 从根节点（编号cnt）开始递归生成编码
	search(cnt);
    
	// 输出每个字符对应的哈夫曼编码
	for(int i = 1; i <= n; ++ i)
		cout << s[i] << " " << chain[i] << "\n";
	
	return 0;
}
```