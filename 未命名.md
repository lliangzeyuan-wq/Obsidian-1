以下是完整的实验代码实现（包含 `lab3.h` 头文件和 `lab3.cpp` 实现文件），严格按照你截图中的函数声明编写：

---

### 1. 头文件定义 (`lab3.h`)

这是你截图中定义宏、结构体和函数声明的部分。

cpp

运行

```
#define STACK_INIT_SIZE  100
#define STACKINCREMENT  10
#define ERROR 0
#define OK  1
#define MAX  100

typedef int SElemType;
typedef int Status;

// 顺序栈结构定义
typedef struct SqStack
{
    SElemType *top;
    SElemType *base;
    int stacksize;
} SqStack;

// 函数声明（与你截图一致）
Status InitStack(SqStack &S);
Status Push(SqStack &S,SElemType e);
Status Pop(SqStack &S,SElemType &e);
Status GetTop(SqStack S,SElemType &e);
Status StackEmpty(SqStack S);
void Conversion();
```

---

### 2. 功能实现 (`lab3.cpp`)

这是实现栈操作和进制转换的核心代码。

cpp

运行

```cpp
#include <iostream>
#include <stdlib.h>
#include <malloc.h>
#include "lab3.h"

using namespace std;

// 1. 初始化栈
Status InitStack(SqStack &S) {
    // 分配初始存储空间
    S.base = (SElemType *)malloc(STACK_INIT_SIZE * sizeof(SElemType));
    if (!S.base) exit(1); // 内存分配失败
    S.top = S.base;      // 栈顶等于栈底表示空栈
    S.stacksize = STACK_INIT_SIZE;
    return OK;
}

// 2. 入栈操作
Status Push(SqStack &S, SElemType e) {
    // 栈满，追加存储空间
    if (S.top - S.base >= S.stacksize) {
        S.base = (SElemType *)realloc(S.base, (S.stacksize + STACKINCREMENT) * sizeof(SElemType));
        if (!S.base) exit(1);
        S.top = S.base + S.stacksize; // 更新栈顶
        S.stacksize += STACKINCREMENT;
    }
    *S.top++ = e; // 先赋值，再栈顶指针上移
    return OK;
}

// 3. 出栈操作
Status Pop(SqStack &S, SElemType &e) {
    if (S.top == S.base) return ERROR; // 栈空
    e = *--S.top; // 栈顶指针先下移，再取值
    return OK;
}

// 4. 获取栈顶元素
Status GetTop(SqStack S, SElemType &e) {
    if (S.top == S.base) return ERROR;
    e = *(S.top - 1); // 注意：S.top不移动，取前一个位置的值
    return OK;
}

// 5. 判断栈是否为空
Status StackEmpty(SqStack S) {
    return S.top == S.base ? OK : ERROR;
}

// 6. 十进制转八进制核心功能
void Conversion() {
    SqStack S;
    SElemType e;
    int num;
    
    InitStack(S); // 初始化栈
    
    cout << "请输入一个非负十进制整数：" << endl;
    
    // 循环输入直到输入合法的非负整数
    while (!(cin >> num) || num < 0) {
        // 处理输入流错误状态
        cin.clear(); 
        while (cin.get() != '\n'); // 清空缓冲区
        cout << "输入非法，请重新输入一个非负十进制整数：" << endl;
    }

    // 特殊情况：输入为0时直接输出
    if (num == 0) {
        cout << "0" << endl;
        return;
    }

    // 入栈：对num反复除8取余
    while (num > 0) {
        Push(S, num % 8); // 余数入栈
        num = num / 8;   // 商作为新的数继续循环
    }

    cout << "输出与其等值的八进制数：" << endl;
    // 出栈：依次输出栈顶元素，即逆序输出余数
    while (!StackEmpty(S)) {
        Pop(S, e);
        cout << e;
    }
    cout << endl; // 换行，符合输出格式
}
```

---

### 3. 测试主程序 (`lab3main.cpp`)

这是你截图中的 `main` 函数入口。

cpp

运行

```
#include <iostream>
#include "lab3.h"

using namespace std;

int main()
{
    Conversion();
    return 0;
}
```

---

### 代码关键点解析

1. **栈空与栈满判断**：
    
    - 栈空：`top == base`
    - 栈满：`top - base >= stacksize`
    
2. **入栈 / 出栈指针移动**：
    
    - `Push`: `*S.top++ = e` (先赋值，指针后移)
    - `Pop`: `e = *--S.top` (指针前移，再取值)
    - `GetTop`: `e = *(S.top - 1)` (不移动指针，取栈顶值)
    
3. **进制转换逻辑**：
    
    - 利用栈 “后进先出” 的特性，将十进制数反复除以 8 得到的余数，最后逆序输出即为八进制数。
    - 处理了`num=0`的边界情况，以及输入非数字或负数的异常情况。