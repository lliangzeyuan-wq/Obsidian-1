****
```
#include <iostream>
#include <cstdlib>
using namespace std;

#define OK 1
#define ERROR 0
#define LIST_INIT_SIZE 100
#define LISTINCREMENT 10

typedef char ElemType;
typedef int Status;

// 顺序表结构体定义
typedef struct
{
    ElemType* elem;
    int length;
    int listsize;
} SqList;

// 1. 初始化顺序表
Status InitList_Sq(SqList& L)
{
    // 分配初始内存
    L.elem = (ElemType*)malloc(LIST_INIT_SIZE * sizeof(ElemType));
    if (!L.elem) exit(-1); // 内存分配失败退出
    L.length = 0;
    L.listsize = LIST_INIT_SIZE;
    return OK;
}

// 2. 在第i个位置前插入元素e（i从1开始）
Status ListInsert_Sq(SqList& L, int i, ElemType e)
{
    // 检查i范围：1<=i<=length+1
    if (i < 1 || i > L.length + 1) return ERROR;
    // 容量不足，扩容
    if (L.length >= L.listsize)
    {
        ElemType* newbase = (ElemType*)realloc(L.elem,
            (L.listsize + LISTINCREMENT) * sizeof(ElemType));
        if (!newbase) exit(-1);
        L.elem = newbase;
        L.listsize += LISTINCREMENT;
    }
    // 元素后移
    ElemType* q = &L.elem[i - 1]; // 插入位置
    for (ElemType* p = &L.elem[L.length - 1]; p >= q; p--)
        *(p + 1) = *p;
    *q = e;
    L.length++;
    return OK;
}

// 3. 删除第i个元素，用e返回被删值
Status ListDelete_Sq(SqList& L, int i, ElemType& e)
{
    if (i < 1 || i > L.length) return ERROR;
    ElemType* p = &L.elem[i - 1];
    e = *p; // 取出删除元素
    // 元素前移覆盖
    ElemType* q = L.elem + L.length - 1;
    for (p++; p <= q; p++)
        *(p - 1) = *p;
    L.length--;
    return OK;
}

// 4. 销毁顺序表
Status DestroyList(SqList& L)
{
    free(L.elem);
    L.elem = nullptr;
    L.length = 0;
    L.listsize = 0;
    return OK;
}

// 5. 判断空表：空返回1，非空返回0
Status ListEmpty(SqList L)
{
    if (L.length == 0)
        return 1;
    else
        return 0;
}

// 主测试函数（题目提供的测试逻辑）
int main()
{
    char A[5] = { 'a','b','c','d','e' }, temp = 'f';
    int i;
    SqList List;
    InitList_Sq(List);

    // 循环插入5个元素
    for (i = 1; i <= 5; i++)
        ListInsert_Sq(List, i, A[i - 1]);

    if (!ListEmpty(List))
        cout << "顺序表非空\n";

    cout << "开始时元素序列为：\n";
    for (i = 1; i <= List.length; i++)
        cout << List.elem[i - 1] << " ";
    cout << endl;

    // 第4位插入'f'
    i = 4;
    ListInsert_Sq(List, i, temp);
    cout << "\n插入后的元素序列为：\n";
    for (i = 1; i <= List.length; i++)
        cout << List.elem[i - 1] << " ";
    cout << endl;

    // 删除第3个元素
    i = 3;
    ListDelete_Sq(List, i, temp);
    cout << "\n删除后的元素序列为：\n";
    for (i = 1; i <= List.length; i++)
        cout << List.elem[i - 1] << " ";
    cout << endl;

    // 销毁表
    if (DestroyList(List))
        cout << "\n成功释放顺序表L";

    return 0;
}
```

## 运行输出结果（和题目样例完全匹配）

plaintext

```
顺序表非空
开始时元素序列为：
a b c d e 

插入后的元素序列为：
a b c f d e 

删除后的元素序列为：
a b f d e 

成功释放顺序表L
```

## 关键说明

1. **下标区分**：题目操作位置 `i` 是**从 1 开始**的逻辑位，数组存储下标 `i-1`；
2. **扩容机制**：插入满时自动新增 `LISTINCREMENT(10)` 空间；
3. **销毁逻辑**：`free` 释放动态数组，清空长度与容量；
4. **判空函数**：长度为 0 返回 1（空），否则返回 0（非空）；
5. **数据类型**：元素是 `char` 字符型，和文档定义 `typedef char ElemType` 一致。

## 编译运行提示

直接复制全部代码到 Dev-C++ / VS / CodeBlocks 即可编译运行，无需拆分文件。

![](https://p26-flow-imagex-sign.byteimg.com/tos-cn-i-a9rns2rl98/bot-chat-image/37e90f6d971f4e369e84fc2437764667~tplv-a9rns2rl98-image.png?lk3s=8e244e95&rcl=20260618162709A7B7609BA5A9ED0F6939&rrcfp=dafada99&x-expires=2097995229&x-signature=rxPqhL0ZeuLUwacKCkEvohKx%2Bg4%3D)

每个东西都代表什么

# 顺序表结构体逐行详解

cpp

运行

```
typedef struct
{
    ElemType* elem;
    int length;
    int listsize;
} SqList;
```

## 1. `typedef struct { ... } SqList;`

- `struct`：定义**结构体**，用来打包一组相关变量，统一描述顺序表；
- `typedef`：给这个结构体起别名 `SqList`；
- 后续写 `SqList L;` 等价于完整写 `struct { ... } L;`，简化代码。
- `SqList` = Sequence List，**顺序表**。

## 2. `ElemType* elem;`

### 拆分开解释

1. `ElemType`：自定义元素类型，前面有 `typedef char ElemType;`，代表顺序表里存的是**字符 char**；想存 int 就改成`typedef int ElemTyp`