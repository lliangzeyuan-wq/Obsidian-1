### ✅ 严格按实验格式要求的纯 C++ 版本（保留实验函数名 / 结构体，替换 C 内存操作为 C++）

cpp

运行

```
#include <iostream>
using namespace std;

// 实验要求的宏定义（保留格式）
#define OK 1
#define ERROR 0
#define LIST_INIT_SIZE 100  // 初始容量
#define LISTINCREMENT 10    // 扩容增量

// 实验要求的类型定义（严格保留格式）
typedef char ElemType;
typedef int Status;

typedef struct
{
    ElemType *elem;   // 动态数组指针
    int length;       // 当前元素个数
    int listsize;     // 当前分配的容量
} SqList;

// 1. 初始化顺序表（实验要求函数名：InitList_Sq）
Status InitList_Sq(SqList &L) {
    // 用C++ new[] 替代 malloc
    L.elem = new(nothrow) ElemType[LIST_INIT_SIZE];
    if (L.elem == nullptr) return ERROR;  // 内存分配失败
    L.length = 0;
    L.listsize = LIST_INIT_SIZE;
    return OK;
}

// 2. 插入元素（实验要求函数名：ListInsert_Sq）
Status ListInsert_Sq(SqList &L, int i, ElemType e) {
    // 检查插入位置合法性
    if (i < 1 || i > L.length + 1) return ERROR;

    // 空间不足时扩容（用C++ new[]+拷贝+delete[] 替代 realloc）
    if (L.length >= L.listsize) {
        ElemType *newbase = new(nothrow) ElemType[L.listsize + LISTINCREMENT];
        if (newbase == nullptr) return ERROR;  // 扩容失败

        // 拷贝旧数据到新数组
        for (int k = 0; k < L.length; ++k) {
            newbase[k] = L.elem[k];
        }
        // 释放旧数组内存
        delete[] L.elem;
        L.elem = newbase;
        L.listsize += LISTINCREMENT;
    }

    // 插入位置及之后元素后移
    ElemType *p = &(L.elem[i-1]);
    for (ElemType *q = &(L.elem[L.length-1]); q >= p; --q) {
        *(q+1) = *q;
    }
    *p = e;
    L.length++;
    return OK;
}

// 3. 删除元素（实验要求函数名：ListDelete_Sq）
Status ListDelete_Sq(SqList &L, int i, ElemType &e) {
    // 检查删除位置合法性
    if (i < 1 || i > L.length) return ERROR;

    ElemType *p = &(L.elem[i-1]);
    e = *p;  // 保存被删除元素的值
    ElemType *q = &(L.elem[L.length-1]);

    // 被删元素之后元素前移
    for (++p; p <= q; ++p) {
        *(p-1) = *p;
    }
    L.length--;
    return OK;
}

// 4. 销毁顺序表（实验要求函数名：DestroyList_Sq）
Status DestroyList_Sq(SqList &L) {
    // 用C++ delete[] 替代 free
    delete[] L.elem;
    L.elem = nullptr;
    L.length = 0;
    L.listsize = 0;
    return OK;
}

// 5. 判断是否为空表（实验要求函数名：ListEmpty）
Status ListEmpty(SqList L) {
    return (L.length == 0) ? 1 : 0;  // 空返回1，非空返回0
}

// ---------------------- 实验要求的测试主函数（严格按格式） ----------------------
int main()
{
    char A[5] = {'a','b','c','d','e'}, temp='f';  // temp用来存放插入/删除的值
    int i;

    SqList List;
    InitList_Sq(List);

    // 批量插入初始元素
    for(i=1; i<=5; i++)
        ListInsert_Sq(List,i,A[i-1]);

    // 判断非空
    if(!ListEmpty(List))
        cout<<"顺序表非空\n";

    // 打印初始序列
    cout<<"\n开始时元素序列为：\n";
    for(i=1; i<= List.length; i++)
        cout<< List.elem[i-1] << " ";

    // 插入元素到第4位
    i=4;
    ListInsert_Sq(List,i,temp);
    cout<<"\n\n插入后的元素序列为：\n";
    for(i=1; i<= List.length; i++)
        cout<<List.elem[i-1] << " ";

    // 删除第3位元素
    i=3;
    ListDelete_Sq(List,i,temp);
    cout<<"\n\n删除后的元素序列为：\n";
    for(i=1; i<= List.length; i++)
        cout<< List.elem[i-1] << " ";

    // 销毁顺序表
    if(DestroyList_Sq(List))
        cout<<"\n\n成功释放顺序表 L.\n";

    return 0;
}
```

---

### 📌 格式说明（完全贴合实验要求）

1. **结构体 / 类型定义**：严格保留实验要求的 `typedef struct`、`ElemType`、`Status` 格式
2. **函数名**：完全使用实验指定的 `InitList_Sq`/`ListInsert_Sq`/`ListDelete_Sq`/`DestroyList_Sq`/`ListEmpty`
3. **测试代码**：和实验样例的 `main` 结构、变量名、输出格式完全一致
4. **内存管理**：
    - 用 `new[]` 替换 `malloc`
    - 用 `delete[]` 替换 `free`
    - 用 `nullptr` 替换 `NULL`