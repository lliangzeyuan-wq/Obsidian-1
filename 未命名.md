`telnet` 是一个老牌的网络客户端工具，你这里用它来测试你的 Muduo 服务器，非常合适。我给你讲清楚它的用法，保证你一看就懂👇

---

## 一、你截图里的命令

bash

运行

```
telnet 127.0.0.1 6000
```

**一句话作用：** 连接到运行在 `127.0.0.1`（本机）上、端口为 `6000` 的服务器，也就是你写的聊天服务器。

---

## 二、`telnet` 的核心用法拆解

### 1. 基本连接命令

bash

运行

```
telnet <服务器IP> <端口号>
```

- `<服务器IP>`：你要连接的服务器地址。
    
    - `127.0.0.1` 就是 “本机”，只能连接你自己电脑上的服务。
    - 如果你服务器部署在其他电脑上，就填它的 IP 地址，比如 `192.168.1.100`。
    
- `<端口号>`：服务器监听的端口，你的代码里写的是 `6000`，所以这里填 `6000`。

连接成功后，会显示类似这样的提示：

text

```
Trying 127.0.0.1...
Connected to 127.0.0.1.
Escape character is '^]'.
```

这就代表你已经成功连上服务器了！

---

### 2. 发送数据

连接成功后，你在终端里输入的任何文字，都会直接发送给服务器。

比如你输入 `hello server`，服务器的 `onMessage` 回调就会收到这条消息，然后把它原封不动回显给你（因为你写的是回声服务器）。

---

### 3. 退出 telnet 客户端

有两种方式：

#### 方式 1：用 telnet 自带的命令

1. 按 `Ctrl + ]`，会出现 `telnet>` 提示符：
    
    text
    
    ```
    ^]
    telnet>
    ```
    
2. 输入 `quit`，按回车，就会断开连接：
    
    text
    
    ```
    telnet> quit
    Connection closed.
    ```
    

#### 方式 2：直接按 `Ctrl + C`

直接强制断开连接，服务器那边也会收到断开事件，执行 `onConnection` 的 `else` 分支，清理连接资源。

---

### 4. 常见问题与说明

- **连接失败**：
    
    - 检查服务器有没有启动（`./server` 有没有运行）。
    - 检查 IP 和端口有没有写错，是不是 `127.0.0.1:6000`。
    - 检查服务器的防火墙有没有拦截 `6000` 端口。
    
- **乱码问题**：
    
    - 如果你输入中文，服务器打印出来是乱码，是因为 `telnet` 默认用的是 ASCII 编码，不影响功能，测试用英文就行。
    

---

## 三、补充：`telnet` 命令的其他常用场景

除了测试你的服务器，`telnet` 还可以用来：

- 测试某个端口是否开放：`telnet 192.168.1.1 80`，如果能连上，说明对方的 80 端口是开放的。
- 手动和 HTTP 服务器交互：比如连接到 80 端口，手动输入 HTTP 请求头。

---

💡 总结：

你现在用 `telnet` 测试你的回声服务器，用法完全正确，它就是一个帮你和服务器建立 TCP 连接、收发数据的工具。

需要我帮你写一个更方便的 Python 客户端，代替 `telnet` 来测试你的服务器吗？这样可以避免乱码，还能发中文消息。