### 代码详细注释（Dsu on Tree 解法：树上启发式合并）

cpp

运行

```cpp
#include "bits/stdc++.h"
using namespace std;
using i64 = long long; // 定义长long，避免溢出

constexpr int N = 2E5; // 颜色最大值（题目颜色范围足够大）

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr); // 加速IO

    int n;
    cin >> n;
    vector<int> c(n), f(n); // c[i]: 节点i的颜色；f[i]: 节点i的父节点
    vector<vector<int>> g(n); // 邻接表存树
    for (int i = 0; i < n; i++) {
        cin >> c[i] >> f[i];
        f[i]--; // 父节点从1-based转0-based
        c[i]--; // 颜色从1-based转0-based（方便数组索引）
        if (f[i] != -1) { // 根节点父节点是-1，不建边
            g[f[i]].push_back(i); // 子节点i加入父节点f[i]的邻接表
        }
    }

    int ans = 0; // 答案：颜色平衡子树的数量
    vector<i64> siz(n); // siz[cur]: 以cur为根的子树大小（用于求重儿子）
    vector<int> l(n), r(n), dfn(n), son(n, -1), cnt(N); 
    // l[cur]/r[cur]: 子树cur在dfn序中的左右区间（子树对应[l[cur], r[cur]]）
    // dfn[tot]: 时间戳tot对应的节点编号（dfn序映射）
    // son[cur]: 节点cur的重儿子（子树最大的子节点），初始-1
    // cnt[color]: 当前统计区间内，颜色color出现的次数

    i64 res = 0, mxc = 0, totcnt = 0; 
    // res: 有多少种颜色的计数等于当前最大值mxc
    // mxc: 当前统计区间内，颜色计数的最大值
    // totcnt: 当前统计区间内，存在的颜色种类数（cnt[color]>0的颜色数）
    int tot = -1; // dfn序的时间戳计数器，初始-1

    // 【添加节点cur的颜色】：更新cnt、totcnt、mxc、res
    auto add = [&](int cur) {
        if (cnt[c[cur]] == 0) { // 该颜色之前没出现过，总颜色数+1
            totcnt++;
        }
        cnt[c[cur]]++; // 当前颜色计数+1
        if (cnt[c[cur]] == mxc) { // 计数等于当前最大值，最大计数的颜色数+1
            res++;
        } else if (cnt[c[cur]] > mxc) { // 计数超过当前最大值，更新最大值，重置最大计数的颜色数为1
            mxc = cnt[ c[cur]];
            res = 1;
        }
    };

    // 【删除节点cur的颜色】：更新cnt、totcnt，重置res和mxc（暴力清理用）
    auto del = [&](int cur) {
        cnt[c[cur]]--; // 当前颜色计数-1
        if (cnt[c[cur]] == 0) { // 该颜色计数归零，总颜色数-1
            totcnt--;
        }
        res = mxc = 0; // 重置最大值和最大计数颜色数（后续会重新add统计）
    };

    // 【第一次DFS】：求子树大小、重儿子、dfn序（标记子树区间）
    function<void(int, int)> dfs = [&](int cur, int pre) {
        l[cur] = ++tot; // 子树cur的左时间戳（进入时间）
        dfn[tot] = cur;  // 时间戳tot对应节点cur
        siz[cur] = 1;    // 子树大小初始为1（自己）
        for (auto &nex : g[cur]) { // 遍历所有子节点
            if (nex != pre) { // 避免回走到父节点
                dfs(nex, cur); // 递归处理子节点
                siz[cur] += siz[nex]; // 累加子树大小
                // 更新重儿子：选子树更大的子节点
                if (son[cur] == -1 || siz[son[cur]] < siz[nex]) {
                    son[cur] = nex;
                }
            }
        }
        r[cur] = tot; // 子树cur的右时间戳（离开时间），子树区间为[l[cur], r[cur]]
    };

    // 【第二次DFS】：Dsu on Tree 核心（树上启发式合并）
    // ok=1: 保留当前子树的统计数据（重儿子）；ok=0: 处理完后清理数据（轻儿子）
    function<void(int, int, bool)> dfs_again = [&](int cur, int pre, bool ok) {
        // 1. 先处理所有轻儿子（不保留数据，ok=0）
        for (auto &nex : g[cur]) {
            if (nex != pre && nex != son[cur]) { // 轻儿子：不是父节点，也不是重儿子
                dfs_again(nex, cur, 0); // 递归处理轻儿子，处理完后清理数据
            }
        }
        // 2. 处理重儿子（保留数据，ok=1）：重儿子的统计数据会被保留，避免重复计算
        if (son[cur] != -1) {
            dfs_again(son[cur], cur, 1);
        }
        // 3. 暴力把所有轻儿子的子树节点加入统计
        for (auto &nex : g[cur]) {
            if (nex != pre && nex != son[cur]) { // 遍历轻儿子
                // 轻儿子的子树是连续区间[l[nex], r[nex]]，逐个add到当前统计
                for (int i = l[nex]; i <= r[nex]; i++) {
                    add(dfn[i]);
                }
            }
        }
        // 4. 把当前节点cur加入统计
        add(cur);
        // 5. 判断当前子树是否为颜色平衡树：
        //    res == totcnt → 所有存在的颜色的计数都等于最大值mxc（即每种颜色个数相同）
        ans += (res == totcnt);
        // 6. 如果不是重儿子（ok=0），清理当前子树的所有节点，避免影响父节点统计
        if (!ok) {
            for (int i = l[cur]; i <= r[cur]; i++) {
                del(dfn[i]);
            }
        }
    };

    dfs(0, -1); // 第一次DFS：根节点是0（原题目根是1，转0-based后是0），父节点-1
    dfs_again(0, -1, 0); // 第二次DFS：根节点ok=0（根节点处理完后不需要保留数据）

    cout << ans << '\n'; // 输出答案

    return 0;
}
```

---

### 核心逻辑总结

1. **Dsu on Tree 思想**：
    
    - 保留**重儿子**的统计数据，避免重复计算，时间复杂度优化到 O(nlogn)。
    - 暴力处理**轻儿子**的子树，统计完后清理数据，保证父节点统计时不受影响。
    
2. **颜色平衡树判断**：
    
    - `res`：计数等于最大值的颜色种类数。
    - `totcnt`：当前子树中存在的颜色种类数。
    - 当 `res == totcnt` 时，说明**所有颜色的计数都等于最大值**，即每种颜色的结点个数相同，是颜色平衡树。
    
3. **子树区间标记**：
    
    - 通过 `dfn` 序将每个子树映射为连续区间 `[l[cur], r[cur]]`，方便暴力遍历子树所有节点。
    

---

### 关键变量 / 函数作用速查

表格

|变量 / 函数|作用|
|---|---|
|`siz[cur]`|子树 `cur` 的大小，用于求重儿子|
|`l[cur]/r[cur]`|子树 `cur` 在 `dfn` 序中的左右区间|
|`son[cur]`|子树 `cur` 的重儿子（子树最大的子节点）|
|`cnt[color]`|当前统计区间内颜色 `color` 的出现次数|
|`mxc`|当前统计区间内颜色计数的最大值|
|`res`|计数等于 `mxc` 的颜色种类数|
|`totcnt`|当前统计区间内存在的颜色种类数（`cnt[color]>0`）|
|`add(cur)`|添加节点 `cur` 的颜色，更新统计变量|
|`del(cur)`|删除节点 `cur` 的颜色，重置统计变量（暴力清理用）|
|`dfs`|求子树大小、重儿子、`dfn` 序|
|`dfs_again`|Dsu on Tree 核心，统计颜色平衡子树数量|

---

要不要我再帮你把这段代码的**时间复杂度分析**和**样例执行过程**也写出来，方便你彻底理解 Dsu on Tree 的优化点？