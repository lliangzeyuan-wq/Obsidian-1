---
data: 2026-03-29
---
### 解决方案（二选一，推荐方案 1）

#### 方案 1：修改前端请求地址为虚拟机 IP（最直接）

1. **在虚拟机里查看自己的 IP 地址**：
    
    打开虚拟机终端，输入：
    
    bash
    
    运行
    
    ```
    ip addr  # 或者 ifconfig
    ```
    
    找到类似 `192.168.x.x` 或 `10.0.x.x` 的内网 IP（比如 `192.168.1.100`）。
    
2. **修改 `api.js` 里的基础 URL**：
    
    把 `API_CONFIG.baseUrl` 从 `http://127.0.0.1:8080` 改成你的虚拟机 IP：
    
    javascript
    
    运行
    
    ```
    const API_CONFIG = {
        baseUrl: 'http://192.168.1.100:8080', // 替换成你的虚拟机IP
        timeout: 10000,
        retryTimes: 3,
        useMock: false
    };
    ```
    
3. **测试连通性**：
    
    在宿主机的终端里输入：
    
    bash
    
    运行
    
    ```
    curl http://192.168.1.100:8080
    ```
    
    如果返回 `{"success":true,"message":"请求成功"}`，说明宿主机能正常访问虚拟机后端了。
    

---

#### 方案 2：配置虚拟机端口转发（适合不想改前端代码）

1. 打开虚拟机设置（以 VMware 为例）：
    
    - 进入「网络适配器」→ 选择「NAT 模式」
    - 点击「高级」→「端口转发」
    - 添加一条规则：
        
        - 主机端口：`8080`
        - 虚拟机 IP：你的虚拟机 IP（比如 `192.168.1.100`）
        - 虚拟机端口：`8080`
        
    
2. 保存后，宿主机访问 `127.0.0.1:8080` 就会自动转发到虚拟机的后端服务。

---

#### 方案 3：把后端搬到宿主机运行（最彻底）

1. 把你的 `server.cpp`、`json.hpp` 和 MySQL 环境都搬到宿主机。
2. 重新编译运行后端：
    
    bash
    
    运行
    
    ```
    g++ server.cpp -o server -lmysqlclient -lcrypto -lpthread
    ./server
    ```
    
3. 前端保持 `http://127.0.0.1:8080` 不变，直接访问即可。

---

### 🧪 验证是否解决

1. 重启后端服务（虚拟机里）
2. 浏览器按 `Ctrl+F5` 强制刷新页面
3. 再次点击「注册」按钮：
    
    - 如果后端终端出现请求日志，且前端弹出「注册成功 / 学号已存在」，说明问题解决 ✅
    - 如果还是报错，把宿主机 `curl` 测试的结果发给我，我帮你进一步排查
    

---

### 💡 补充：如果是防火墙问题

如果虚拟机里能访问后端，但宿主机还是访问不了，可能是虚拟机防火墙拦截了 8080 端口：

bash

运行

```
# 临时关闭防火墙（测试用）
sudo ufw disable
# 或者开放8080端口
sudo ufw allow 8080/tcp
```

---

**总结**：你现在的问题不是代码 bug，而是**虚拟机网络导致的「请求根本到不了后端」**，只要把前端的请求地址改成虚拟机 IP，或者配置端口转发，就能立刻解决！

需要我帮你一步步指导怎么查虚拟机 IP 吗？

思考

编程

图像生成

帮我写作

翻译

视频生成

**更多**