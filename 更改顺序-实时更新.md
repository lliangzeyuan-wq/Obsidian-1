---
data: 2026-03-29
---
## 第二步：确认前端接口地址写对了（第二常见的问题）

你的前端页面运行在 `127.0.0.1:5500`（VSCode 的 Live Server），后端服务在 `127.0.0.1:8080`，**必须写完整的接口地址，不能用相对路径**。

1. 打开前端项目里的 `js/api.js` 文件
2. 确认所有接口的请求地址，都是完整的格式，比如：
    
    javascript
    
    运行
    
    ```
    // ✅ 正确写法：写全后端地址
    const baseUrl = "http://127.0.0.1:8080";
    
    // 个人资料接口示例
    export function getUserProfile(userId) {
      return fetch(`${baseUrl}/api/profile/${userId}`, {
        headers: {
          "Authorization": "Bearer " + localStorage.getItem("token")
        }
      })
    }
    ```
    
    ❌ 错误写法（只写相对路径）：
    
    javascript
    
    运行
    
    ```
    // 错误：会请求到 127.0.0.1:5500/api/profile，这个端口没有后端服务
    fetch("/api/profile/xxx")
    ```
    

---

## 第三步：刷新页面，看 2 个关键面板的报错

保持后端服务运行，刷新个人资料页面，打开 F12 开发者工具：

### 1. 先看「Console（控制台）」面板

有没有红色的 JS 报错？比如：

- `fetch is not defined`
- `token is undefined`
- 跨域报错 `CORS error`
- 404/500 错误

### 2. 再看「Network（网络）」面板

刷新页面后，看有没有出现请求：

- **完全没有请求**：前端 JS 代码没触发接口调用，大概率是 JS 报错导致代码中断执行，先修 Console 里的报错。
- **有红色的请求**：
    
    - 状态码 `404`：接口地址写错了，或者后端没有这个接口，核对地址和后端的接口路径。
    - 状态码 `500`：后端接口执行崩溃，看后端终端的报错日志，会打印具体的 SQL 错误 / 代码错误。
    - `CORS跨域错误`：前端地址和后端端口不一致，之前的代码已经加了跨域头，重启后端服务即可解决。
    

---

## 第四步：确认前端请求带了 Token

个人资料接口必须登录后才能访问，需要在请求头里带 Token：

1. 先退出重新登录一次，确保登录成功后，`localStorage`里存了`token`
2. 在 Network 面板里，点击个人资料的请求，看「Request Headers」里有没有：
    
    plaintext
    
    ```
    Authorization: Bearer 你的token
    ```
    
    如果没有，就是前端请求没带 Token，后端会直接返回「请先登录」，前端没处理就会显示「网络异常」。

---

## 最后验证

按上面的步骤排查完后，刷新页面，看后端终端有没有打印：

plaintext

```
【个人资料接口】查询用户ID：xxx
```

如果打印了，说明请求已经到后端了，根据终端的报错再调整即可；如果没打印，就是前端的地址 / 请求没写对。

你可以把 Console 和 Network 里的报错截图发给我，我帮你精准定位剩余问题。