---
data: 2026-03-29
---
从截图能看到注册请求一直处于 `Pending`（挂起）状态，这是**后端程序未正确启动 / 端口未监听** 导致的核心问题，不是代码本身的问题！

我帮你一步步排查 + 解决，按顺序操作就能立刻恢复：

### 🚨 第一步：检查后端是否真的启动成功

1. 打开运行后端的终端，确认是否有这行输出：
    
    `✅ MySQL 数据库连接成功`
    
    `✅ 服务器启动成功！端口：8080`
    
    → 如果没有，说明后端没启动，执行下一步
    
2. 重新编译 + 启动后端（必须确保无报错）：
    
    bash
    
    运行
    
    ```
    # 先关闭旧的后端进程（按 Ctrl + C）
    g++ server.cpp -o server -lmysqlclient -lcrypto -lpthread  # 编译
    ./server  # 启动
    ```
    
    ✅ 编译成功的标志：终端无任何报错，只显示上面两行成功提示
    

### 🚨 第二步：检查端口是否被占用（8080 端口被占也会导致 Pending）

1. 查看 8080 端口占用情况：
    
    bash
    
    运行
    
    ```
    # Linux/Mac
    lsof -i:8080
    # Windows（cmd）
    netstat -ano | findstr 8080
    ```
    
2. 如果有进程占用，杀死该进程：
    
    bash
    
    运行
    
    ```
    # Linux/Mac（替换 PID 为查到的进程号）
    kill -9 PID
    # Windows（替换 PID 为查到的进程号）
    taskkill /F /PID PID
    ```
    
3. 重新启动后端：`./server`

### 🚨 第三步：前端请求地址是否正确（关键！）

检查你的 `api.js` 文件中，注册接口的请求地址是否是：

`http://localhost:8080/api/register`

→ 不是的话，改成这个地址！

### 🚨 第四步：测试后端是否能访问（终极验证）

打开浏览器，访问：`http://localhost:8080`

→ 如果能看到 `{"success":true,"message":"请求成功"}`，说明后端正常；

→ 如果浏览器显示「无法访问」，说明后端没启动 / 端口错了！

### 🚨 第五步：前端代码小优化（确保请求不卡死）

修改你的 `api.js` 中注册函数，添加超时和错误捕获：

javascript

运行

```
// 注册接口
async function registerUser(data) {
  try {
    const response = await fetch('http://localhost:8080/api/register', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data),
      timeout: 5000  // 5秒超时
    });
    if (!response.ok) throw new Error('网络错误');
    return await response.json();
  } catch (e) {
    showToast('注册失败：后端未启动或端口错误');
    console.error(e);
    return { success: false };
  }
}
```

---

### ✅ 最终操作顺序（必按）

1. 关闭所有终端窗口 → 重新打开终端
2. 编译后端：`g++ server.cpp -o server -lmysqlclient -lcrypto -lpthread`
3. 启动后端：`./server` → 确认显示「MySQL 连接成功 + 服务器启动成功」
4. 浏览器清空缓存 → 刷新登录页
5. 重新尝试注册

### 🎯 成功标志

- 后端终端能看到请求日志
- 前端注册请求状态从 `Pending` 变成 `200`
- 弹出「注册成功」提示

如果还是不行，把后端终端的**完整输出**截图发给我，我帮你定位问题！