---
data: 2026-03-29
---
## 一、必须立刻修复的 3 个核心错误（按优先级）

### 1. 确认后端连接的数据库是 `partner`（最容易踩的坑）

打开你的后端 C++ 代码，找到 MySQL 连接的代码，必须是这样的：

cpp

运行

```
// 第5个参数是数据库名，必须写 partner，不能是campus_social！
mysql_real_connect(
    mysql, 
    "127.0.0.1", 
    "你的MySQL用户名",  // 一般是root
    "你的MySQL密码", 
    "partner",  // 必须是这个！不能写错
    3306, 
    NULL, 
    0
);
```

⚠️ 如果你这里写的是 `campus_social`（空库），会导致所有 SQL 执行失败，代码把「SQL 执行错误」当成了「学号已存在」，所以无论输入什么都报错。

---

### 2. 修复「学号检查」的 SQL 语句（字段名必须和表对应）

你的`user`表**只有`id`字段存学号**，没有`student_id`字段，所以必须把代码里所有的`student_id`改成`id`。

❌ 错误写法（你代码里大概率是这样）：

cpp

运行

```
// 错误：表中根本没有student_id字段，SQL会直接执行失败
const char* check_sql = "SELECT * FROM user WHERE student_id = ?";
```

✅ 正确写法（和你的表结构完全匹配）：

cpp

运行

```
// 正确：id就是你表里存学号的主键字段
const char* check_sql = "SELECT id FROM user WHERE id = ?";
```

---

### 3. 修复错误的判断逻辑（不能把 SQL 执行失败当成「学号已存在」）

你之前的代码大概率把「SQL 执行报错」和「查到重复学号」的逻辑搞混了，必须按下面的逻辑重写：

cpp

运行

```
// 1. 初始化预处理语句
MYSQL_STMT* stmt = mysql_stmt_init(mysql);
if (!stmt) {
    cout << "【调试】stmt初始化失败：" << mysql_error(mysql) << endl;
    return json{{"success", false}, {"message", "数据库初始化失败"}};
}

// 2. 预处理SQL（用id字段）
const char* check_sql = "SELECT id FROM user WHERE id = ?";
if (mysql_stmt_prepare(stmt, check_sql, strlen(check_sql)) != 0) {
    cout << "【调试】SQL预处理失败：" << mysql_stmt_error(stmt) << endl;
    mysql_stmt_close(stmt);
    return json{{"success", false}, {"message", "SQL预处理失败"}};
}

// 3. 绑定学号参数（前端传的studentId就是学号，对应id字段）
MYSQL_BIND param;
memset(&param, 0, sizeof(param));
param.buffer_type = MYSQL_TYPE_STRING;
param.buffer = (void*)studentId.c_str(); // studentId是前端传的学号
unsigned long len = studentId.length();
param.length = &len;

if (mysql_stmt_bind_param(stmt, &param) != 0) {
    cout << "【调试】参数绑定失败：" << mysql_stmt_error(stmt) << endl;
    mysql_stmt_close(stmt);
    return json{{"success", false}, {"message", "参数绑定失败"}};
}

// 4. 执行查询
if (mysql_stmt_execute(stmt) != 0) {
    cout << "【调试】SQL执行失败：" << mysql_stmt_error(stmt) << endl;
    mysql_stmt_close(stmt);
    // 这里绝对不能返回「学号已存在」！这是SQL执行错误！
    return json{{"success", false}, {"message", "数据库查询失败，请重试"}};
}

// 5. 获取查询结果的行数
mysql_stmt_store_result(stmt);
int row_count = mysql_stmt_num_rows(stmt);
cout << "【调试】查询到的行数：" << row_count << endl; // 这里必须是0！

// 6. 核心判断：行数>0 = 学号已存在
if (row_count > 0) {
    cout << "【调试】学号已存在，返回错误" << endl;
    mysql_stmt_close(stmt);
    return json{{"success", false}, {"message", "注册失败（学号已存在）"}};
}
mysql_stmt_close(stmt);

// 7. 到这里说明学号不存在，执行插入新用户的逻辑
cout << "【调试】学号不存在，开始执行插入" << endl;
// 这里写你的INSERT插入代码，字段名必须和user表对应！
```

---

## 二、修复后的验证步骤

1. 把上面的代码替换掉你后端的注册逻辑
2. 重新编译后端：
    
    bash
    
    运行
    
    ```
    make clean && make
    ```
    
3. 重启后端服务：
    
    bash
    
    运行
    
    ```
    ./server
    ```
    
4. 前端重新点击注册，同时看后端终端的**调试打印输出**
    
    - 如果打印 `【调试】查询到的行数：0`，就会正常执行插入，注册成功
    - 如果有报错，直接看终端的错误信息，就能精准定位问题
    

---

## 三、补充：插入语句的正确写法

你的插入 SQL 也必须和表结构对应，示例：

cpp

运行

```
const char* insert_sql = "INSERT INTO user (id, name, gender, password) VALUES (?, ?, ?, ?)";
```

⚠️ 字段名必须和`DESC user;`查到的完全一致，不能多写、不能写错。