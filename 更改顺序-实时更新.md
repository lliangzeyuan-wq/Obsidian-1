---
data: 2026-03-29
---
## 第一步：先确认后端服务 100% 正常（最核心的基础）

前端所有的「网络异常」，90% 都是后端没正常工作，先做这个验证：

1. 打开你的 Linux 终端，确保后端服务正在运行，终端里必须显示这行：
    
    plaintext
    
    ```
    ✅ MySQL 数据库连接成功，服务器启动成功！端口：8080
    ```
    
    ⚠️ 这个终端绝对不能关闭，最小化放后台就行。
2. 直接在浏览器地址栏输入这个地址，按回车：
    
    plaintext
    
    ```
    http://127.0.0.1:8080/api/stats
    ```
    
    - ✅ 正常结果：浏览器显示一串 JSON 数据，比如 `{"success":true,"data":{"friends":5,"matches":12,"chats":8}}` → 后端完全正常，问题在前端。
    - ❌ 异常结果：无法访问、404、空白页 → 后端没启动成功 / 端口被占用，先重新编译运行后端：
        
        bash
        
        运行
        
        ```
        make clean && make
        ./server
        ```
        
        看终端有没有报错，把报错发给我。
    

---

## 第二步：定位前端「网络异常」的根因（必做）

后端确认正常后，打开个人资料页，按 `F12` 打开开发者工具，按顺序看 2 个面板：

### 1. 先看【Console（控制台）】面板

有没有**红色的报错文字**？比如：

- `CORS 跨域错误`
- `token is undefined`
- `fetch failed`
- 404/500 错误
    
    把报错内容完整截图 / 抄下来，这是定位问题的关键。

### 2. 再看【Network（网络）】面板

1. 刷新个人资料页，在请求列表里找**红色的、名字带`profile`的请求**
2. 点这个请求，看这几个关键信息：
    
    - **Request URL**：是不是 `http://127.0.0.1:8080/api/profile/xxx` ？如果地址不对，就是`api.js`的`baseUrl`没改对。
    - **Status（状态码）**：是 200/401/404/500？
        
        - 401：没带 token，或者 token 无效，需要重新登录
        - 500：后端接口执行崩溃，看 Linux 终端的报错日志
        - CORS 错误：跨域问题，重启后端服务即可
        
    - **Request Headers**：有没有 `Authorization: Bearer 你的token` 这一行？如果没有，就是前端没带 token，后端直接拒绝了请求。
    - **Response**：后端返回了什么内容？是报错信息还是正常数据？
    

---

## 第三步：解决「退出登录按钮消失」的问题

这个问题 99% 是**连锁反应**：

前端的退出登录按钮，是有「条件渲染」的（比如只有拿到用户信息、登录状态正常，才会显示按钮）。现在接口报错，页面拿不到用户数据，按钮的显示条件不满足，自然就消失了。

等你把接口调通，页面能正常拿到个人资料数据，退出登录按钮会自动恢复显示。如果接口通了按钮还没出来，你把`profile.html`的代码发给我，我帮你看按钮的渲染逻辑。

---

## 补充：必做的前置操作

1. 必须**先退出登录，回到登录页，重新输入学号密码登录一次**，确保登录成功后，`localStorage`里存了有效的`token`。
2. 绝对不能直接在浏览器打开`profile.html`，必须从登录页登录后，点底部「我的」跳转进入，否则页面拿不到登录信息，接口必然报错。

你先按第一步测后端接口，把结果和 F12 的报错发给我，我帮你精准解决剩余问题。