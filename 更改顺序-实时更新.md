---
data: 2026-03-29
---
### 是的！**后端运行日志就是在这里看** ✅

你现在的终端里：

- 只打印了 `MySQL 数据库连接成功`、`服务器启动成功！端口：8080`
- **完全没有** 打印「更新资料接口」相关的日志（比如 `【更新资料接口】更新用户ID：xxx`）

这说明：**你点「提交」后，请求根本没到达后端**，所以后端没输出任何日志，前端才会一直卡在「提交中...」。

---

## 🔍 为什么请求没到后端？（3 个核心原因）

### 原因 1：前端 `fetch` 的 URL / 端口写错了

打开 `profile-edit.html`，检查 `submitProfile` 里的请求地址：

javascript

运行

```
const response = await fetch('http://localhost:8080/api/profile/update', {
    // ...
});
```

必须满足：

- 端口是 `8080`（和后端启动的端口一致）
- 路径是 `/api/profile/update`（不能少 `/api`，不能拼错）
- 不能写成 `127.0.0.1` 或其他端口

---

### 原因 2：浏览器跨域拦截（最常见！）

你的前端页面是 `127.0.0.1:5500`，后端是 `localhost:8080`，浏览器会默认阻止跨域请求。

**修复方法**：在后端 `server.cpp` 的 `resp_headers` 里加上跨域允许头：

cpp

运行

```
string resp_headers =
    "HTTP/1.1 200 OK\r\n"
    "Content-Type: application/json; charset=utf-8\r\n"
    "Access-Control-Allow-Origin: *\r\n"  // 允许所有源跨域
    "Access-Control-Allow-Methods: GET, POST, OPTIONS\r\n"
    "Access-Control-Allow-Headers: Content-Type, Authorization\r\n"
    "Connection: close\r\n\r\n";
```

加完后**重新编译后端**，再测试。

---

### 原因 3：前端 `token`/`userId` 缺失，请求没发出去

在浏览器 `Console` 里输入：

javascript

运行

```
console.log(localStorage.getItem('token'));
console.log(localStorage.getItem('userId'));
```

如果输出 `null`，说明你没登录，或者登录后没存 `token`/`userId`，前端会直接 `return`，不发请求。

---

## 🚀 最快验证：先测「后端是否能收到请求」

1. 后端加一个**测试接口**（在 `handleClient` 里）：
    
    cpp
    
    运行
    
    ```
    else if (path == "/api/hello") {
        resp_json["success"] = true;
        resp_json["message"] = "后端收到请求啦！";
        string resp = resp_headers + resp_json.dump();
        send(client_fd, resp.c_str(), resp.size(), 0);
        close(client_fd);
        pthread_exit(NULL);
    }
    ```
    
2. 前端 `submitProfile` 里临时改成：
    
    javascript
    
    运行
    
    ```
    const response = await fetch('http://localhost:8080/api/hello');
    ```
    
3. 点「提交」，如果：
    
    - 后端终端打印了新日志 → 请求通了
    - 前端弹出「后端收到请求啦！」→ 跨域 / URL 问题解决了
    - 还是卡 → 问题在其他地方
    

---

### 📌 你现在要做的

先把 **浏览器 Console 的报错**（如果有红色 `CORS error`）和 **Network 面板里的请求状态** 发给我，我能 10 秒内定位是「跨域」「URL 错了」还是「没存 token」，然后给你最终修复代码！