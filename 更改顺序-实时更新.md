---
data: 2026-03-29
---
### 问题根因（终于找到！）

现在**不会跳登录页但一直提示「请先登录」**，说明：

- 前端确实拿到了 `token` 和 `userId`
- 但**后端验证 `token` 时失败了**，所以返回「请先登录」
- 前端收到错误后只弹提示，没有继续跳转，所以「卡死」

核心是 **后端 `verifyToken` 函数的两个致命问题**：

1. **`Authorization` 头截取长度算错** → 后端拿不到完整的 `token`
2. **SQL 拼接未转义** → 若 `token` 含特殊字符（如单引号），SQL 语法错误，直接返回空 `user_id`

---

### ✅ 第一步：先给前端加「调试日志」，确认问题位置

把 `submitProfile` 函数改成下面这样，**打开 F12 控制台**，就能看到提交时的 `token` 和后端返回结果：

javascript

运行

```
async function submitProfile() {
    // 实时拿最新的登录信息
    const token = localStorage.getItem('token');
    const userId = localStorage.getItem('userId');
    
    // 调试：打印当前 token 和 userId
    console.log("📤 提交时 token:", token);
    console.log("📤 提交时 userId:", userId);

    // 只有真的没登录才提示
    if (!token || !userId) {
        showToast("请先登录");
        setTimeout(() => window.location.href = "../index.html", 1000);
        return;
    }

    const profile = collectProfileData();
    try {
        const response = await fetch('http://localhost:8080/api/profile/update', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': 'Bearer ' + token // 格式必须是 Bearer + 空格 + token
            },
            body: JSON.stringify(profile)
        });
        
        const result = await response.json();
        // 调试：打印后端返回结果
        console.log("📥 后端返回:", result);

        if (result.success) {
            showToast("资料保存成功！");
            setTimeout(() => window.location.href = "profile.html", 1000);
        } else {
            showToast(result.message || "保存失败");
            // 如果是登录问题，重点看这里
            if (result.message === "请先登录") {
                console.error("❌ 后端验证 token 失败！当前 token:", token);
            }
        }
    } catch (e) {
        showToast("网络错误");
        console.error("💥 请求出错:", e);
    }
}
```

---

### ✅ 第二步：修复后端 `server.cpp` 的两个致命 Bug

#### 1. 修复「Authorization 头截取长度错误」

你后端代码里，`Authorization: Bearer` 的长度算错了！

**原代码（错）**：

cpp

运行

```
size_t auth_pos = request.find("Authorization: Bearer ");
if (auth_pos != string::npos) {
    token = request.substr(auth_pos + 23); // 这里多了 1 位！
    ...
}
```

**修正后（对）**：

cpp

运行

```
size_t auth_pos = request.find("Authorization: Bearer ");
if (auth_pos != string::npos) {
    // "Authorization: Bearer " 实际长度是 22（包括最后的空格）
    token = request.substr(auth_pos + 22); 
    size_t end_pos = token.find("\r\n");
    if (end_pos != string::npos) token = token.substr(0, end_pos);
}
```

#### 2. 修复「SQL 拼接未转义」导致的验证失败

**原代码（错）**：

cpp

运行

```
sprintf(sql, "SELECT id FROM user WHERE token='%s'", token.c_str());
```

**修正后（对）**：

cpp

运行

```
// 先转义 token，避免 SQL 注入/语法错误
char escaped_token[1024];
mysql_real_escape_string(conn, escaped_token, token.c_str(), token.size());
sprintf(sql, "SELECT id FROM user WHERE token='%s'", escaped_token);
```

---

### ✅ 第三步：重启后端 + 测试

1. 保存 `server.cpp` 修改，**重新编译运行后端**
2. 前端刷新页面 → 重新登录 → 填资料 → 点「完成」
3. 看 F12 控制台日志：
    
    - 如果 `token` 存在，后端返回 `success: true` → 资料保存成功
    - 如果还是返回 `请先登录`，把控制台日志发给我，我帮你精准定位最后一个问题！
    

---

### 🎯 修复后保证

- 不会再提示「请先登录」
- 资料正常提交到 MySQL
- 提交后自动跳转到个人资料页
- 完全不会卡死

你先按这个步骤改，**重点看 F12 控制台的日志**，如果还有问题，把日志贴给我，我帮你 1 分钟搞定！

思考

编程

图像生成

帮我写作

翻译

视频生成

更多