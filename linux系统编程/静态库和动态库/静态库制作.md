---
data: 2026-03-31
---
1.讲.c 生成 .o 文件
		`gcc -c add.c -o add.o`

2.使用 `ar` 工具制作静态库
		`ar rcs lib库名.a file.o`
3.编译静态库到可执行文件中：
		`gcc FILENAME.c lib库名.a -o FILENAME`
		`gcc FILENAME.c -o FILENAME -lxxx -L[路径]`