---
data: 2026-03-12
---
 -  `-I`  :  指定头文件所在位置，后面跟头文件所在位置的绝对或相对路径，用不用空格隔开都无所谓
	 - `gcc hello.c -o hello -I./inc` : 表示添加了一个头文件搜索路径，当前目录的inc文件夹中
	 - `gcc -I./inc hello.c -o hello` :  -I的位置也可以放在这里

- `-L`  :
	-当你用 `-lxxx` 链接一个库（比如 `-lredis++`）时，编译器会按固定顺序去这些路径找 `libxxx.so` 或 `libxxx.a`：

	1. 系统默认库路径（`/lib`、`/usr/lib`、`/usr/local/lib` 等）
	2. 你用 `-L` 指定的路径
	`-L` 的作用，就是**给编译器额外增加一个库文件的搜索目录**，告诉它：“除了系统默认路径，也去这里找找库文件”。


  
  `-c` : 只做预处理、编译、汇编。得到二进制文件！！！
  `-g` : 编译时添加调试文件。从而支持gdb调试
  `-Wall` ： 提示更多警告信息。操作系统会把警告分优先级，当出现这个的时候，不管你的警告的优先级，他会把所有的警告列出来， Wall->warning all
  `-D` : 向程序中”动态“注册宏定义

# 示例
- 生成二进制文件
`gcc -c add.c -o add.o`
- 生成可执行文件
`gcc add.c -o add`



![[Pasted image 20260509160542.png]]