### 好消息！你的数据**已经成功保存到数据库了**！

从截图里能清晰看到：

- `id = 2025300446`
- `name = 梁泽远`
- `gender = male`
- `major = 软件工程`
- `grade = 大二`

---

### 🚨 问题出在「后端查询接口 + 前端渲染」

现在数据在数据库里躺着，但前端拿不到，原因只有两个：

#### 1. 后端查询接口没把数据放到 `data` 里返回

你的后端 `/api/profile/{userId}` 接口现在只返回了：

json

```
{ "success": true, "message": "请求成功" }
```

**根本没把 `user` 表的数据放到 `data` 字段里**，所以前端拿不到任何资料。

✅ 修复后端代码（核心）：

打开 `server.cpp`，找到查询接口，修改为：

cpp

运行

```
// 1. 正确的 SQL：查 user 表，用 id 做条件
std::string sql = "SELECT * FROM user WHERE id = ?";

// 2. 执行查询后，把数据放到 JSON 的 data 里
Json::Value result;
result["success"] = true;
result["message"] = "请求成功";

if (mysql_num_rows(res) > 0) {
    MYSQL_ROW row = mysql_fetch_row(res);
    Json::Value data;
    data["name"] = row[19];       // 对应 name 字段
    data["gender"] = row[20];    // 对应 gender 字段
    data["grade"] = row[4];      // 对应 study_grade（年级）字段
    data["major"] = row[7];      // 对应 postgrad_major（专业）字段
    // ... 其他你需要的字段（study_subject、friend_hobby 等）
    result["data"] = data;
} else {
    result["data"] = Json::Value(Json::nullValue);
}
```

#### 2. 前端渲染代码没正确读取 `data` 里的字段

打开 `profile.html`，确保渲染代码是：

javascript

运行

```
async function fetchUserInfo() {
  const token = localStorage.getItem('token');
  const userId = localStorage.getItem('userId');
  const res = await fetch(`http://localhost:8080/api/profile/${userId}`, {
    headers: { 'Authorization': 'Bearer ' + token }
  });
  const result = await res.json();
  
  if (result.success && result.data) {
    // 把后端返回的 data 渲染到页面
    document.getElementById('name').textContent = result.data.name;
    document.getElementById('gender').textContent = result.data.gender || '--';
    document.getElementById('grade').textContent = result.data.grade || '--';
    document.getElementById('major').textContent = result.data.major || '--';
    // ... 其他字段
  }
}
```

---

### 🎯 修复后验证

1. 重新编译后端：`make`
2. 重启服务：`./server`
3. 前端刷新「我的」页面 → 就能看到你填的**梁泽远、男、大二、软件工程**等信息了！

---

### ✅ 总结

- **保存成功**：数据已经在 `user` 表里了 ✅
- **查询失败**：后端没把数据放到 `data` 里返回 ❌
- **显示失败**：前端没正确读取 `data` 里的字段 ❌

改完这两处，你的「我的」页面就会正常显示资料啦！

需要我帮你把**完整的后端查询接口代码**和**前端渲染代码**都写好吗？直接替换就能跑～