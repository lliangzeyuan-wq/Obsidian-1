### 核心问题诊断

从截图可以看到：你的虚拟机网卡 **ens33 状态为 DOWN**（未激活），且没有分配 IP 地址。

**根本原因**：

你虽然设置了 **桥接模式**，但虚拟机启动时没有自动获取新的网络配置（DHCP 分配失败），导致网络处于断开状态。

---

### ✅ 解决步骤（按顺序执行）

#### 步骤 1：重启网络服务（最快尝试）

在虚拟机终端执行以下命令，强制重启网络：

bash

运行

```
# 重启网络管理器
sudo systemctl restart NetworkManager

# 或者尝试自动获取IP
sudo dhclient -v
```

- 看到 `DHCPACK` 字样说明获取成功。
- 若仍无 IP，继续下一步。

---

#### 步骤 2：手动启动网卡（最有效）

终端执行：

bash

运行

```
# 启动 ens33 网卡
sudo ifconfig ens33 up

# 或者使用 ip 命令
sudo ip link set ens33 up
```

执行后，再输入 `ip addr`，看 ens33 是否变成 `LOWER_UP` 并分配了 IP。

---

#### 步骤 3：静态配置 IP（一劳永逸，桥接模式首选）

如果自动获取一直失败，手动配置静态 IP（与宿主机同网段）：

1. **查看宿主机 IP**：
    
    在宿主机（Windows）按 `Win + R` 输入 `cmd`，执行 `ipconfig`，找到你的局域网网关（比如 `192.168.1.1`）。
    
2. **虚拟机配置静态 IP**：
    
    编辑网卡配置文件：
    
    bash
    
    运行
    
    ```
    sudo nano /etc/netplan/00-installer-config.yaml
    ```
    
3. **粘贴以下配置**（根据你的宿主机网段修改）：
    
    假设你的宿主机是 `192.168.1.x` 网段：
    
    yaml
    
    ```
    network:
      ethernets:
        ens33:
          addresses: [192.168.1.131/24]  # 虚拟机IP，与宿主机同网段
          gateway4: 192.168.1.1           # 宿主机网关
          nameservers:
            addresses: [8.8.8.8, 114.114.114.114]
      version: 2
    ```
    
    > ⚠️ 注意：把 `192.168.1.131` 改成你的新 IP，确保和宿主机不冲突！
    
4. **生效配置**：
    
    bash
    
    运行
    
    ```
    sudo netplan apply
    ```
    

---

#### 步骤 4：最终验证

1. 在虚拟机终端：`ip addr`
    
    - 看到 `ens33: <BROADCAST,MULTICAST,UP,LOWER_UP>`
    - 看到 `inet 192.168.1.x` 地址 ✅
    
2. 在虚拟机测试：
    
    bash
    
    运行
    
    ```
    curl http://127.0.0.1:8080
    ```
    
    正常返回。
    
3. 在宿主机浏览器：
    
    text
    
    ```
    http://192.168.1.131:8080
    ```
    
    能正常访问后端，注册功能就通了！
    

---

### 💡 总结

现在的问题不是代码，而是**虚拟机网卡没起来**。

- 先执行 `sudo ifconfig ens33 up` 试试。
- 不行就按步骤 **3 配置静态 IP**，这是最稳定的开发环境。

配置好新 IP 后，记得把你的 `api.js` 里的 `baseUrl` 改成这个新 IP！