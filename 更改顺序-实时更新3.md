从截图可以看到：

1. 你之前按了 `Ctrl+C` 停止了后端服务，所以现在**后端没有在运行**，导致端口 8080 无人监听。
2. 即使后端运行，虚拟机防火墙默认也会**拦截外部对 8080 端口的访问**，所以宿主机连接被拒绝。

---

### ✅ 解决步骤（按顺序执行）

#### 1. 重新启动后端服务

在虚拟机终端执行：

bash

运行

```
./server
```

✅ 确认终端输出：`✅ MySQL 数据库连接成功` + `✅ 服务器启动成功！端口：8080`

---

#### 2. 确认后端监听地址（验证后端本身正常）

在虚拟机新终端执行：

bash

运行

```
curl http://127.0.0.1:8080
```

✅ 预期结果：返回 `{"success":true,"message":"请求成功"}`

→ 说明后端在虚拟机内部正常运行。

---

#### 3. 开放虚拟机防火墙 8080 端口

在虚拟机终端执行：

bash

运行

```
# 开放8080端口
sudo ufw allow 8080/tcp
# 查看防火墙状态
sudo ufw status
```

✅ 确认输出中包含 `8080/tcp ALLOW Anywhere`

---

#### 4. 验证宿主机访问

在**宿主机**终端 / 浏览器执行：

bash

运行

```
curl http://192.168.198.131:8080
```

✅ 预期结果：返回 `{"success":true,"message":"请求成功"}`

→ 此时宿主机可以正常访问虚拟机后端了。

---

### 💡 补充：如果仍无法访问

1. **检查后端监听配置**：确认 `server.cpp` 中 `server_addr.sin_addr.s_addr = htonl(INADDR_ANY);`（监听所有网卡，不是仅 127.0.0.1）
2. **检查虚拟机网络模式**：
    
    - 若为 NAT 模式：需在虚拟机设置中添加「端口转发」（主机 8080 → 虚拟机 192.168.198.131:8080）
    - 若为桥接模式：确保宿主机和虚拟机在同一网段（如 192.168.198.x）
    

---

### 最终效果

完成以上步骤后，前端访问 `http://192.168.198.131:8080` 就不会再出现「连接被拒绝」，注册 / 登录功能即可正常使用。

需要我帮你检查 `server.cpp` 里的监听地址配置是否正确吗？

思考

编程

图像生成

帮我写作

翻译

视频生成

更多