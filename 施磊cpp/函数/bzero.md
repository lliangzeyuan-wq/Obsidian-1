---
data: 2026-08-11
---
# 函数原型

**作用：把内存全部清零（全部变成 0）**。

```
void bzero(void *s, size_t n);
```

两个参数：

表格

|参数|意思|你代码里的值|
|---|---|---|
|`s`|要清零的内存起始地址|`buf.get()`（缓冲区的地址）|
|`n`|清零多少字节|`4096`（整个缓冲区的大小）|
