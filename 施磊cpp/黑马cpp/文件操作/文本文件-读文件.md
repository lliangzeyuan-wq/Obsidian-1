---
data: 2026-03-05
---
```cpp
#include<iostream>
using namespace std;
//1.头文件包含
#include<fstream>//与文件操作有关的头文件
#include<string>

//文本文件 写文件

void test01()
{
	//2.创建流对象
	//ofstream(写文件),ifstream(写文件),fstream(读写)
	ifstream ifs;//这里你用ifstream和fstream都可以


	//3.打开文件 并且判断是否打开成功
	ifs.open("test.txt", ios::in);
	if (!ifs.is_open())
	{
		cout << "文件打开失败" << endl;
		return;
	}


	//4.读数据
	
	
	//第一种
	char buf[1024] = { 0 };
	//  >>是 “流提取运算符” 作用是提取数据
	// cin>> 是从键盘读取数据   ifs>> 是从文件提取数据
	while (ifs >> buf)
	{
		cout << buf << endl; 
	}
	
	////第二种
	//char buf[1024] = { 0 };
	//while (ifs.getline(buf, sizeof(buf)))
	//{
	//	cout << buf << endl;
	//}

	//第三种
	//string buf;
	//while (getline(ifs, buf))
	//{
	//	cout << buf << endl;
	//}

	////第四种
	//char c;
	//while ((c = ifs.get()) != EOF)
	//{
	//	cout << c;
	//}


	//5.关闭文件
	ifs.close();
}

int main()
{
	test01();
	return 0;
}
```