### 找到了！你的 `server.cpp` 里有**3 个致命错误**，导致无论输入什么学号都提示「已存在」：

---

## 错误 1：数据库名写错了（最核心）

在代码第 20 行左右：

cpp

运行

```
// ❌ 错误：这里写的是 campus_social（空库），应该是 partner！
#define DB_NAME "campus_social"
```

✅ 必须改成：

cpp

运行

```
#define DB_NAME "partner"
```

---

## 错误 2：表名写错了

你的代码里所有 SQL 都用的是 `users` 表（带 s），但你 `partner` 库里的表名是 `user`（没有 s）！

❌ 错误示例（代码里到处都是）：

cpp

运行

```
// 错误：表名是 users，但数据库里是 user
sprintf(sql, "SELECT id, name, gender FROM users WHERE student_id='%s' AND password='%s'", ...);
```

✅ 必须把代码里**所有的 `users` 都改成 `user`**（注意是所有 SQL 语句里的表名）。

---

## 错误 3：字段名写错了（最关键）

你的代码里用的是 `student_id` 字段，但你 `partner` 库的 `user` 表里，**学号字段是 `id`**，根本没有 `student_id` 这个字段！

❌ 错误示例：

cpp

运行

```
// 错误：字段名是 student_id，但数据库里学号字段是 id
sprintf(sql, "SELECT id, name, gender FROM users WHERE student_id='%s' AND password='%s'", ...);
```

✅ 必须把代码里**所有的 `student_id` 都改成 `id`**。

---

## 错误 4：注册接口的逻辑（补充优化）

你的注册接口没有先做「学号重复检查」，直接 INSERT，如果 INSERT 失败（主键冲突），就返回错误，这逻辑本身没问题，但因为上面的库名、表名、字段名全错了，导致 INSERT 永远失败，所以永远提示「学号已存在」。

---

# ✅ 完整修复方案（按顺序操作）

## 第一步：修改数据库名

找到第 20 行左右的宏定义，改成：

cpp

运行

```
#define DB_NAME "partner"
```

## 第二步：全局替换表名和字段名

在 VSCode 里按 `Ctrl + H` 打开全局替换：

1. **替换表名**：
    
    - 查找：`FROM users`
    - 替换为：`FROM user`
    - 点击「全部替换」
    
2. **替换字段名**：
    
    - 查找：`student_id`
    - 替换为：`id`
    - 点击「全部替换」
    

## 第三步：修复注册接口的 INSERT 语句

找到注册接口的 INSERT 语句（大概在第 200 行左右），改成和你的 `user` 表结构对应：

cpp

运行

```
// 正确：id就是学号，对应你前端传的studentId
sprintf(sql, "INSERT INTO user (id, name, gender, password) VALUES ('%s','%s','%s','%s')",
    studentId.c_str(), name.c_str(), gender.c_str(), pwd.c_str());
```

## 第四步：重新编译运行

修改完成后，执行：

bash

运行

```
make clean && make
./server
```

---

## 🧪 验证修复

1. 确保后端终端打印 `✅ MySQL 数据库连接成功`
2. 前端重新点击注册
3. 如果还是报错，看后端终端的报错信息，就能精准定位剩余问题

现在按这个方案修改，注册功能就能正常工作了！