---
data: 2026-02-22
---

```cpp
#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string s;
    cin >> s;
    int n = s.size();
    if (n == 0) {
        cout << 0 << endl;
        return 0;
    }

    // f[i] 表示从 i 开始的最长连续非递减子串长度
    vector<int> f(n, 1);
    for (int i = n - 2; i >= 0; --i) {
        if (s[i + 1] == s[i] || s[i + 1] == s[i] + 1) {
            f[i] = f[i + 1] + 1;
        } else {
            f[i] = 1;
        }
    }

    long long ans = n; // 长度为 1 的好串

    // 枚举拆分点 k (0 <= k < n-1)，子串为 s[0..k] 和 s[k+1..n-1]
    for (int k = 0; k < n - 1; ++k) {
        // 左边从 0 到 k，右边从 k+1 开始
        int left_len = k + 1;
        int right_max = f[k + 1]; // 右边从 k+1 开始的最长长度

        // 左边能取的长度 l 满足：1 <= l <= left_len，且 s[0..l-1] 是连续非递减
        // 等价于 l <= f[0]
        int left_max = f[0];
        int max_l = min(left_max, left_len);

        // 对于每个 l (1 <= l <= max_l)，右边可以取 1..right_max 长度
        ans += (long long)max_l * right_max;
    }

    cout << ans << endl;

    return 0;
}
```