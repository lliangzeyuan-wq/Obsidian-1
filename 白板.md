---
data: 2026-02-22
---
```c++
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <arpa/inet.h>
#include <sys/socket.h>

// ===================== 协议相关的常量定义 =====================
#define BUF_SIZE    1024    // 通用缓冲区大小
#define RLT_SIZE    4       // 服务器返回的运算结果长度（4字节int）
#define OPSZ        4       // 每个操作数的长度（4字节int）

// 错误处理函数声明
void error_handling(char *message);

// ===================== 主函数：运算客户端 =====================
int main(int argc, char *argv[])
{
    int sock;                    // 客户端套接字描述符
    char opmsg[BUF_SIZE];       // 统一存储待发送数据的缓冲区（char数组）
    int result, opnd_cnt, i;     // result: 服务器返回的结果；opnd_cnt: 操作数个数；i: 循环变量
    struct sockaddr_in serv_adr;// 服务器地址结构体

    // 1. 检查命令行参数（必须传入服务器IP和端口）
    if(argc != 3)
    {
        printf("Usage: %s <IP> <port>\n", argv[0]);
        exit(1);
    }

    // 2. 创建TCP套接字
    sock = socket(PF_INET, SOCK_STREAM, 0);
    if(sock == -1)
        error_handling("socket() error");

    // 3. 初始化服务器地址结构体
    memset(&serv_adr, 0, sizeof(serv_adr));
    serv_adr.sin_family = AF_INET;
    serv_adr.sin_addr.s_addr = inet_addr(argv[1]); // 服务器IP地址
    serv_adr.sin_port = htons(atoi(argv[2]));       // 服务器端口号（主机字节序转网络字节序）

    // 4. 连接服务器
    if(connect(sock, (struct sockaddr*)&serv_adr, sizeof(serv_adr)) == -1)
        error_handling("connect() error!");
    else
        puts("Connected...........");

    // ===================== 5. 用户输入阶段 =====================
    // 5.1 输入操作数个数（最多255个，1字节存储）
    fputs("Operand count: ", stdout);
    scanf("%d", &opnd_cnt);

    // 5.2 把“操作数个数”写入发送缓冲区的第1个字节（协议的第一个字段）
    opmsg[0] = (char)opnd_cnt;

    // 5.3 依次读取每个操作数，并写入发送缓冲区
    for(i = 0; i < opnd_cnt; i++)
    {
        printf("Operand %d: ", i+1);
        scanf("%d", (int*)&opmsg[i*OPSZ + 1]); 
        // 解释：opmsg是char数组，按偏移写入4字节的int数据
        // 第1个字节是opnd_cnt，后面每4字节存一个操作数
    }

    // 5.4 读取运算符（+、-、*），写入缓冲区
    fgetc(stdin); // 清除前面scanf留下的换行符\n，避免被fgets读取
    fputs("Operator: ", stdout);
    scanf("%c", &opmsg[opnd_cnt*OPSZ + 1]);
    // 解释：操作数个数占1字节，后面opnd_cnt个操作数占 opnd_cnt*OPSZ 字节
	    // 运算符就放在这些数据的后面，占1字节
	
	    // ===================== 6. 发送请求数据 =====================
	    // 发送的总长度 = 1(操作数个数) + opnd_cnt*OPSZ(操作数) + 1(运算符)
    write(sock, opmsg, opnd_cnt*OPSZ + 2);

    // ===================== 7. 接收服务器返回的运算结果 =====================
    // 服务器会返回一个4字节的int结果，循环read直到收完
    int recv_len = 0;
    while (recv_len < RLT_SIZE) {
        int recv_cnt = read(sock, &result + recv_len, RLT_SIZE - recv_len);
        if(recv_cnt == -1)
            error_handling("read() error!");
        recv_len += recv_cnt;
    }

    // ===================== 8. 输出结果并关闭连接 =====================
    printf("Operation result: %d \n", result);
    close(sock);

    return 0;
}

// ===================== 错误处理函数 =====================
void error_handling(char *message)
{
    fputs(message, stderr);
    fputc('\n', stderr);
    exit(1);
}
```

---

### 🔍 关键知识点拆解（对应书中的讲解）