---
data: 2026-02-22
---
```cpp
// 通用头文件，重复内容不额外注释
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <arpa/inet.h>
#include <sys/socket.h>

// ========== 【应用层协议核心参数，重点注释】 ==========
#define BUF_SIZE 1024  // 通用缓冲区大小
#define RLT_SIZE 4      // 约定：运算结果的字节长度，int类型固定4字节
#define OPSZ 4          // 约定：每个操作数的字节长度，int类型固定4字节
// 以上两个宏是客户端和服务器提前约定的协议规则，解决TCP数据无边界问题

void error_handling(char *message); // 错误处理函数，和之前示例完全一致，不重复注释

int main(int argc, char *argv[])
{
    int sock;
    // ========== 【协议核心：数据包缓冲区】 ==========
    // opmsg是我们自定义协议的数据包载体，用char数组是为了按字节精准控制数据包格式
    // 可以兼容存储不同类型的数据（1字节的个数/运算符、4字节的int操作数）
    char opmsg[BUF_SIZE];
    int result, opnd_cnt, i;
    struct sockaddr_in serv_adr;

    // 启动参数检查，重复内容不额外注释
    if(argc!=3) {
        printf("Usage : %s <IP> <port>\n", argv[0]);
        exit(1);
    }

    // socket创建、地址初始化、connect连接，和回声客户端完全一致，不重复注释
    sock=socket(PF_INET, SOCK_STREAM, 0);
    if(sock==-1)
        error_handling("socket() error");
    
    memset(&serv_adr, 0, sizeof(serv_adr));
    serv_adr.sin_family=AF_INET;
    serv_adr.sin_addr.s_addr=inet_addr(argv[1]);
    serv_adr.sin_port=htons(atoi(argv[2]));

    if(connect(sock, (struct sockaddr*)&serv_adr, sizeof(serv_adr))==-1)
        error_handling("connect() error!");
    else
        puts("Connected..........");

    // ========== 【应用层协议实现核心，重点注释】 ==========
    // 1. 先获取操作数的个数，这是我们协议的"包头"，服务器拿到后就能算出整个数据包的长度
    fputs("Operand count: ", stdout);
    scanf("%d", &opnd_cnt);
    opmsg[0] = (char)opnd_cnt; // 数据包第1个字节：固定存储操作数的个数（1字节char）

    // 2. 循环输入每个操作数，按协议格式打包到数据包中
    for(i=0; i<opnd_cnt; i++)
    {
        printf("Operand %d: ", i+1);
        // 数据包从第2个字节开始，每4个字节存储1个int类型的操作数
        // 强制类型转换：把char数组的对应地址转为int*，让scanf按4字节int写入，实现数据打包
        scanf("%d", (int*)&opmsg[i*OPSZ + 1]);
    }
    fgetc(stdin); // 吃掉缓冲区残留的换行符，避免影响后续运算符的输入

    // 3. 输入运算符，放到数据包的末尾
    fputs("Operator: ", stdout);
    scanf("%c", &opmsg[opnd_cnt*OPSZ + 1]);
    // 运算符存储位置：操作数个数(1字节) + 所有操作数(个数*4字节) 的下一个字节，固定1字节

    // ========== 【协议数据包发送】 ==========
    // 一次性发送完整的数据包，长度是我们协议约定好的：
    // 1字节(操作数个数) + opnd_cnt*4字节(所有操作数) + 1字节(运算符) = 总长度
    write(sock, opmsg, opnd_cnt*OPSZ + 2);

    // ========== 【协议结果接收】 ==========
    // 提前和服务器约定好：运算结果固定是4字节的int类型，直接读满4字节即可
    // 不用再找结束符，彻底解决TCP字节流无边界的问题
    read(sock, &result, RLT_SIZE);

    // 输出运算结果
    printf("Operation result: %d \n", result);
    close(sock);
    return 0;
}

// 错误处理函数，和之前示例完全一致，不重复注释
void error_handling(char *message)
{
    fputs(message, stderr);
    fputc('\n', stderr);
}
```