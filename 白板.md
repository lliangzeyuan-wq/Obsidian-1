---
data: 2026-02-22
---
```cpp
class Solution {

public:

    bool isPalindrome(ListNode* head) {
	   vectot<int>vec;
	   ListNode*temp=head;
	   while(temp!=nullptr)
	   {
		   vec.emplace_back(temp->val);
		   temp=temp->next;
	   }
	   for(int i=0,j=vec.size()-1;i<j;i++,j--){
		   if(vec[i]!=vec[j])
		   {
			   return false;
		   }
	   }
	   retrun true;
    }

};
```