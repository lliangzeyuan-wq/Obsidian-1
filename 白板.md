---
data: 2026-02-22
---
```cpp
//反转链表
ListNode*reverseList(ListNode*head)
{
	ListNode*prev=nullptr;
	ListNode*curr=head;
	while(curr!=nullptr)
	ListNode*nextTemp=curr->next;
	curr->next=prev;
	prev=curr;
	curr=nextTemp;
}
```