---
data: 2026-02-22
---
```cpp
#include "bits/stdc++.h"
using namespace std;
using i64 = long long;

//  constexpr 定义最大节点数（编译期常量，安全、高效）
constexpr int N = 2E5;

int main() {
    // 加速 cin/cout，比赛必写
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    vector<int> c(n), f(n);       // c=颜色  f=父节点
    vector<vector<int>> g(n);     // 树的邻接表

    for (int i = 0; i < n; i++) {
        cin >> c[i] >> f[i];
        f[i]--;      // 父节点转 0 编号（方便数组）
        c[i]--;      // 颜色转 0 编号（防止越界，方便 cnt 数组）

        if (f[i] != -1) {
            g[f[i]].push_back(i);  // 把当前节点挂到父节点下面
        }
    }

    int ans = 0;                    // 最终答案：平衡子树的数量
    vector<i64> siz(n);             // 子树大小
    vector<int> l(n), r(n);         // l[u] = 子树开头时间戳
                                    // r[u] = 子树结束时间戳
    vector<int> dfn(n);             // 时间戳 -> 节点编号
    vector<int> son(n, -1);         // 重儿子，初始-1
    vector<int> cnt(N);             // 统计每种颜色出现多少次

    i64 res = 0, mxc = 0;           // res = 有多少种颜色达到最大次数
                                    // mxc = 当前出现最多的颜色次数
    int totcnt = 0;                 // 当前子树有多少种不同颜色
    int tot = -1;                   // 时间戳从-1开始自增

    // ==========================
    // add：把节点 cur 加入统计
    // ==========================
    auto add = [&](int cur) {
        // 如果这个颜色之前没出现过，颜色种类+1
        if (cnt[c[cur]] == 0) {
            totcnt++;
        }
        cnt[c[cur]]++;  // 颜色计数+1

        // 更新最大次数信息
        if (cnt[c[cur]] == mxc) {
            res++;  // 又多一种颜色达到最大值
        } 
        else if (cnt[c[cur]] > mxc) {
            mxc = cnt[c[cur]];  // 新的最大值
            res = 1;            // 只有它自己是最大
        }
    };

    // ==========================
    // del：删除节点 cur 的贡献
    // ==========================
    auto del = [&](int cur) {
        cnt[c[cur]]--;  // 颜色计数-1

        // 颜色变成0，种类-1
        if (cnt[c[cur]] == 0) {
            totcnt--;
        }
        // 清空最大值信息（删除后要重新算）
        res = mxc = 0;
    };

    // ==========================
    // 第一次DFS：求重儿子 + 时间戳
    // ==========================
    function<void(int, int)> dfs = [&](int cur, int pre) {
        l[cur] = ++tot;        // 子树开始时间戳
        dfn[tot] = cur;        // 记录时间戳对应节点
        siz[cur] = 1;          // 子树大小初始为1（自己）

        // 遍历所有孩子
        for (auto &nex : g[cur]) {
            if (nex != pre) {
                dfs(nex, cur);
                siz[cur] += siz[nex];  // 累加子树大小

                // 更新重儿子：选子树最大的那个
                if (son[cur] == -1 || siz[son[cur]] < siz[nex]) {
                    son[cur] = nex;
                }
            }
        }
        r[cur] = tot;  // 子树结束时间戳
    };

    // ==========================
    // 第二次DFS：树上启发式合并
    // ==========================
    function<void(int, int, bool)> dfs_again = [&](int cur, int pre, bool ok) {
        // 1. 先递归所有【轻儿子】，算完清空（ok=0）
        for (auto &nex : g[cur]) {
            if (nex != pre && nex != son[cur]) {
                dfs_again(nex, cur, 0);
            }
        }

        // 2. 递归【重儿子】，算完保留（ok=1）
        if (son[cur] != -1) {
            dfs_again(son[cur], cur, 1);
        }

        // 3. 暴力把所有轻儿子的信息加进来
        for (auto &nex : g[cur]) {
            if (nex != pre && nex != son[cur]) {
                // 遍历轻儿子的整个子树，加入统计
                for (int i = l[nex]; i <= r[nex]; i++) {
                    add(dfn[i]);
                }
            }
        }

        // 4. 把自己也加进去
        add(cur);

        // =====================
        // 核心判断：平衡子树
        // res == 颜色总数
        // → 所有颜色出现次数一样多 → 平衡 ✅
        // =====================
        ans += (res == totcnt);

        // 5. 如果不是重儿子，用完清空！（关键！）
        if (!ok) {
            for (int i = l[cur]; i <= r[cur]; i++) {
                del(dfn[i]);
            }
        }
    };

    // 跑两次DFS
    dfs(0, -1);
    dfs_again(0, -1, 0);

    cout << ans << '\n';

    return 0;
}
```

---




### 1. 单文件版本（推荐：目前只有 server.c）

如果你的目录里只有 `server.c`（服务端代码），直接把下面内容复制进去，保存为 `Makefile`（注意没有后缀）。

makefile

```cpp
# 定义编译参数：开启所有警告(-Wall) + 生成调试信息(-g)
# 这和你手动敲 -Wall -g 是一样的
CFLAGS = -Wall -g

# 定义目标程序名（最终生成的可执行文件叫 "server"）
TARGET = server

# 定义源文件（自动查找当前目录下所有的 .c 文件，这里只有 server.c）
SRCS = $(wildcard *.c)

# 将 .c 后缀替换成 .o 后缀（生成 server.o）
OBJS = $(patsubst %.c, %.o, $(SRCS))

# 1. 默认目标：执行 make 时直接编译出服务器
all: $(TARGET)

# 2. 生成目标程序 server
# 依赖是 server.o，gcc 会把它链接成可执行文件
$(TARGET): $(OBJS)
	$(CC) $^ -o $@ $(CFLAGS)

# 3. 生成 .o 文件
# 规则：任何 .c 文件变化，都会重新编译
%.o: %.c
	$(CC) -c $< -o $@ $(CFLAGS)

# 4. 清理命令：执行 make clean 删除生成的文件
clean:
	rm -rf $(OBJS) $(TARGET)

# 5. 声明伪目标，防止目录里有名为 clean/all 的文件冲突
.PHONY: all clean
```

---

### 2. 双文件版本（如果有 client.c）
