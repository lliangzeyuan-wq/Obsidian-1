---
data: 2026-02-22
---
```cpp
class Solution
{
public:
	
	//反转链表
	ListNode*reverseList(ListNode*head)
	{
		ListNode*pre=nullptr;
		ListNode*cur=head;
		while(cur!=nullptr)
		{
			ListNode*next=cur->next;
			cur->next=pre;
			pre=cur;
			cur=next;
		}
		return pre;
	}
	
	//偶数返回中间偏左，奇数返回正中间
	ListNode*endOfFirstHalf(ListNode*head)
	{
	}
	
	bool isPalindrome(ListNode*head)
	{
		if(head==nullptr)
		{
			return true;
		}
		ListNode*firstEnd=endOfFirstHalf(head);//长
		ListNode*secondStart=reverseList(firstEnd->first);//短
		bool result=true;
		while(result&&secondStart!=nullptr)
		{
			if(firstEnd->val!=secondStart->val)
			{
				result=false;
			}
			
		}
	}
	
}
```