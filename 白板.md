---
data: 2026-02-22
---
```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 9;
int a[N], n, dp[N]; // dp[i] 表示长度为i的不下降子序列的最小尾元素


    return 0;
}
    cin >> n;
    dp[0] = INT_MIN; // 初始化极小值，保证第一个元素能正确替换
    for (int i = 1; i <= n; ++i) {
        cin >> a[i];
        dp[i] = INT_MAX; // 其余初始化为极大值
    }
    
    int ans = 0;
    for (int i = 1; i <= n; ++i) {
        int l = 0, r = n + 1;
        // 开区间二分：找第一个 > a[i] 的位置（upper_bound）
        while (l + 1 != r) {
            int mid = (l + r) >> 1;
            if (dp[mid] > a[i])
                r = mid;
            else
                l = mid;
        }
        dp[r] = a[i]; // 更新对应长度的最小尾元素
        ans = max(r, ans); // 更新最长长度
    }
    
    cout << ans << '\n';
```