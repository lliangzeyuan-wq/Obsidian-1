---
data: 2026-02-22
---
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    long long S;
    cin >> n >> S;

    vector<pair<int, int>> soldiers(n);
    for (int i = 0; i < n; ++i) {
        int p, c;
        cin >> p >> c;
        soldiers[i] = {c, p}; // 存储为 (训练次数, 单次成本)，便于排序
    }

    // 按训练次数升序排序
    sort(soldiers.begin(), soldiers.end());

    // 预处理后缀和：sum_pi[i] 表示从第i个士兵到最后的单次成本和
    // sum_ci_pi[i] 表示从第i个士兵到最后的 (训练次数 × 单次成本) 和
    vector<long long> sum_pi(n + 1, 0);
    vector<long long> sum_ci_pi(n + 1, 0);
    for (int i = n - 1; i >= 0; --i) {
        sum_pi[i] = sum_pi[i + 1] + soldiers[i].second;
        sum_ci_pi[i] = sum_ci_pi[i + 1] + (long long)soldiers[i].first * soldiers[i].second;
    }

    long long min_cost = LLONG_MAX;
    int prev_t = -1;

    // 遍历所有关键 t 值：0 和所有 c_i
    for (int i = 0; i <= n; ++i) {
        int t;
        if (i == 0) {
            t = 0;
        } else {
            t = soldiers[i - 1].first;
        }

        // 跳过重复的 t 值，避免重复计算
        if (t == prev_t) {
            continue;
        }
        prev_t = t;

        // 找到第一个训练次数 > t 的士兵位置
        auto it = upper_bound(soldiers.begin(), soldiers.end(), make_pair(t, INT_MAX));
        int pos = it - soldiers.begin();

        // 计算当前 t 对应的总花费
        long long current = (long long)t * S + (sum_ci_pi[pos] - (long long)t * sum_pi[pos]);
        if (current < min_cost) {
            min_cost = current;
        }
    }

    cout << min_cost << endl;

    return 0;
}
```

```cpp
#include <bits/stdc++.h>
using namespace std;
#define ll long long 
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    ll S;
    cin>>n>>S;
    vector<pair<int,int>>soldiers(n);
    for(int i=0;i<n;++i)
    {
    	//p费用，c次数
    	int p,c;
    	cin>>p>>c;
    	soldiers[i]={c,p};
    	soldiers[i]=make_pair(c,p);
    	//按照（次数，费用）排序，方便
    	soldiers[i]=pair<int,int>(c,p);
	}
	//按照训练次数排序
	sort(soldiers.begin(),soldiers.end());
	vector<long long>sum_pi(n+1,0);
	vector<long long>sum_ci_pi(n+1,0);
	//为什么要预处理：可以递推，省时间
	for(int i=n-1;i>=0;--i)
	{
		sum_pi[i]=sum_pi[i+1]+soldiers[i].second;
		sum_ci_pi[i]=sum_ci_pi[i+1]+(long long)soldiers[i].first*soldiers[i].second;
	}
	long long min_cost=LLONG_MAX;
	int prev_t=-1;//???
	
	//遍历所有的关键的t值：0和所有的c_i
	for(int i=0;i<=n;++i)
	{
		int t;
		if(i==0)
		{
			//t从0开始
			t==0;
		}
		else
		{
			//i的1~n对应了soldiers下标的0~(n-1)
			t=soldiers[i-1].first;
		}
		if(t==prev_t)
		{
			continue;
		}
		prev_t=t;
		//找到第一个训练次数>t的士兵位置,不是=，是>
		auto it=upper_bound(soldiers.begin(),soldiers.end(),make_pair(t,INT_MAX));
		int pos=it-soldiers.begin();
		//计算当前t对应的花费
		ll current=(ll)t*S+(sum_ci_pi[pos]-(ll)t*sum_pi[pos]);
		if(current<min_cost)
		{
			min_cost=current;
		}
	}
	cout<<min_cost<<endl;
    return 0;
}
```