---
data: 2026-02-22
---
```cpp
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

// ==================== 全局变量定义 ====================
const int N = 1e5 + 9;       // 数组最大长度，适配n<=1e5的规模
int n, k, m;                 // n:序列长度, k:可修改的连续数个数, m:离散化后不同元素的个数
int a[N], b[N];              // a[]:原始输入数组; b[]:用于离散化的临时数组
int dp[N];                   // dp[i]: 以a[i]结尾的「前缀最长不下降子序列长度」（1~i区间）
int t[N << 2];               // t[]:线段树数组，开4倍大小，用于维护区间最大值


// ==================== 线段树：单点更新函数 ====================
/**
 * @brief 线段树单点更新：将pos位置的值更新为max(当前值, val)
 * @param o: 当前线段树节点编号（根节点为1）
 * @param l: 当前节点覆盖的区间左端点
 * @param r: 当前节点覆盖的区间右端点
 * @param pos: 要更新的位置（离散化后的下标）
 * @param val: 要更新的值（对应位置的最长不下降子序列长度）
 */
void updata(int o, int l, int r, int pos, int val) {
    t[o] = max(t[o], val);   // 维护区间最大值：当前节点取「原值」和「新值」的较大者
    if (l == r) return;       // 递归终止：到达叶子节点
    int mid = (l + r) >> 1;   // 计算区间中点（等价于(l+r)/2，向下取整）
    // 根据pos位置，递归更新左/右子树
    if (pos <= mid) updata(2*o, l, mid, pos, val);
    else updata(2*o+1, mid+1, r, pos, val);
}


// ==================== 线段树：区间查询最大值函数 ====================
/**
 * @brief 线段树区间查询：查询[nl, nr]区间的最大值
 * @param o: 当前线段树节点编号
 * @param l: 当前节点覆盖的区间左端点
 * @param r: 当前节点覆盖的区间右端点
 * @param nl: 查询区间的左端点
 * @param nr: 查询区间的右端点
 * @return 区间[nl, nr]的最大值
 */
int getmax(int o, int l, int r, int nl, int nr) {
    // 当前区间完全包含在查询区间内，直接返回节点值
    if (nl <= l && r <= nr) return t[o];
    int mid = (l + r) >> 1;
    int res = 0;
    // 左子区间与查询区间有交集，递归查询左子树
    if (mid >= nl) res = getmax(2*o, l, mid, nl, nr);
    // 右子区间与查询区间有交集，递归查询右子树，取最大值
    if (mid < nr) res = max(res, getmax(2*o+1, mid+1, r, nl, nr));
    return res;
}


// ==================== 主逻辑函数 ====================
void solve() {
    // -------------------- 1. 输入数据 --------------------
    cin >> n >> k;
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
        b[i] = a[i];  // 复制原数组到b，用于后续离散化（不修改原数组a）
    }

    // -------------------- 2. 离散化：压缩数值范围 --------------------
    // 作用：将原数组a[i]（范围可能到1e9）映射为1~m的连续下标，适配线段树
    sort(b + 1, b + n + 1);          // 对临时数组b排序，为去重做准备
    m = unique(b + 1, b + n + 1) - (b + 1);  // 去重，m为去重后不同元素的个数
    // 将原数组a[i]映射为离散化后的1-based下标（保留数值大小关系）
    for (int i = 1; i <= n; i++) {
        a[i] = lower_bound(b + 1, b + m + 1, a[i]) - b;
    }

    // -------------------- 3. 预处理前缀dp数组 --------------------
    // dp[i] = 以a[i]结尾的「1~i区间最长不下降子序列长度」
    memset(t, 0, sizeof t);  // 清空线段树，准备计算前缀
    for (int i = 1; i <= n; i++) {
        // 查询[1, a[i]]区间的最大值，+1就是以a[i]结尾的最长不下降子序列长度
        dp[i] = getmax(1, 1, m, 1, a[i]) + 1;
        // 更新线段树：将a[i]位置的值更新为max(当前值, dp[i])
        updata(1, 1, m, a[i], dp[i]);
    }

    // -------------------- 4. 清空线段树，准备处理后缀 --------------------
    memset(t, 0, sizeof t);
    int ans = 0;  // 最终答案：最长不下降子序列的最大长度

    // -------------------- 5. 从后往前遍历，枚举修改段 --------------------
    // 循环条件i > k：保证修改段长度为k（i-k >=1，修改段起点i-k+1 >=1）
    for (int i = n; i > k; i--) {
        // ============== 核心公式：总长度 = 前缀 + 中间 + 后缀 ==============
        // dp[i-k]：前缀1~i-k的最长不下降子序列长度（以a[i-k]结尾）
        // k：中间修改的k个连续数（统一改为a[i-k]，完美衔接前缀）
        // getmax(1,1,m,a[i-k],m)：后缀i+1~n中，>=a[i-k]的最长不下降子序列长度（衔接中间）
        ans = max(ans, dp[i - k] + k + getmax(1, 1, m, a[i - k], m));

        // ============== 维护后缀信息：将a[i]加入线段树 ==============
        // getmax(1,1,m,a[i],m)+1：以a[i]开头的「i~n区间最长不下降子序列长度」
        int tem = getmax(1, 1, m, a[i], m) + 1;
        updata(1, 1, m, a[i], tem);  // 更新线段树，维护i~n的信息
    }

    // -------------------- 6. 特殊处理：修改前k个元素的情况 --------------------
    // 当修改前k个时，前缀为空，直接计算「中间k个 + 后缀」
    // k：中间修改的k个连续数（统一改为a[k+1]，衔接后缀）
    // getmax(1,1,m,a[k+1],m)：后缀k+1~n中>=a[k+1]的最长不下降子序列长度
    ans = max(ans, k + getmax(1, 1, m, a[k + 1], m));

    // -------------------- 7. 输出最终答案 --------------------
    cout << ans << '\n';
}


// ==================== 主函数 ====================
int main() {
    // 关闭C++与C的输入输出同步，加速读写（竞赛必备）
    ios::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);
    int q = 1;
    // cin >> q;  // 多组数据时打开此注释
    while (q--) {
        solve();
    }
    return 0;
}
```

---

## 🎯 核心逻辑补充说明

### 1. 为什么要离散化？

原数组`a[i]`的数值范围可能达到`1e6`甚至`1e9`，无法直接作为线段树的下标。离散化将**大范围、小数量**的数值压缩为`1~m`的连续下标，同时保留数值的大小关系，完美适配线段树。

### 2. 为什么用线段树？

线段树可以在`O(logn)`时间内完成**单点更新**和**区间最大值查询**，将最长不下降子序列的时间复杂度从朴素的`O(n²)`优化为`O(nlogn)`，适配`n=1e5`的规模。

### 3. 三段式拆分的核心思想

当修改连续`K`个数时，将序列分为 3 部分：

- **前缀段**：`1 ~ i-k`（不修改，用`dp[i-k]`存最长长度）
- **中间段**：`i-k+1 ~ i`（修改为`a[i-k]`，保证衔接前缀，长度固定为`K`）
- **后缀段**：`i+1 ~ n`（不修改，用线段树查询`>=a[i-k]`的最长长度，衔接中间）

### 4. 特殊处理说明

- **修改前`K`个**：前缀为空，直接计算`K + 后缀长度`
- **修改后`K`个**：后缀为空，循环中`i`