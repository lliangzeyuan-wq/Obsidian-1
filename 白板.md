---
data: 2026-02-22
---
```cpp
#include <iostream>
#include <vector>
#include <set>
#include <algorithm> // abs()

using namespace std;

// 简化 long long 写法
typedef long long ll;

// 自己实现最大公约数 gcd，不依赖编译器内置函数，C++11 通用
ll gcd(ll a, ll b) {
    a = abs(a);
    b = abs(b);
    while (b != 0) {
        ll temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}

// 直线结构体：用一般式 Ax + By + C = 0 表示
// 保证同一条直线 A、B、C 唯一且最简
struct Line {
    ll A, B, C;

    // 由两点 (x1,y1) (x2,y2) 构造直线
    Line(int x1, int y1, int x2, int y2) {
        // 直线一般式公式推导
        ll a = (ll)y2 - y1;
        ll b = (ll)x1 - x2;
        ll c = (ll)x2 * y1 - (ll)x1 * y2;

        // 对 a,b,c 同时除以最大公约数，化为最简系数
        ll g = gcd(gcd(a, b), c);
        if (g > 0) {
            a /= g;
            b /= g;
            c /= g;
        }

        // 统一符号：保证最前面非零系数为正，避免同一直线两种表示
        if (a != 0) {
            if (a < 0) {
                a = -a;
                b = -b;
                c = -c;
            }
        } else if (b != 0) {
            if (b < 0) {
                a = -a;
                b = -b;
                c = -c;
            }
        }

        // 赋值给结构体成员
        A = a;
        B = b;
        C = c;
    }

    // 重载 < 运算符，使 Line 可以放入 set 进行去重
    bool operator<(const Line& other) const {
        if (A != other.A) return A < other.A;
        if (B != other.B) return B < other.B;
        return C < other.C;
    }
};

int main() {
    // 存储所有网格点：x从0~19，y从0~20
    vector<pair<int, int>> points;
    for (int x = 0; x < 20; ++x) {
        for (int y = 0; y < 21; ++y) {
            points.emplace_back(x, y);
        }
    }

    // set 自动去重，存放所有不同直线
    set<Line> st;

    // 枚举所有点对 i < j，避免重复
    int n = points.size();
    for (int i = 0; i < n; ++i) {
        int x1 = points[i].first;
        int y1 = points[i].second;
        for (int j = i + 1; j < n; ++j) {
            int x2 = points[j].first;
            int y2 = points[j].second;
            // 构造直线并插入集合
            st.insert(Line(x1, y1, x2, y2));
        }
    }

    // set 的大小就是不同直线的数量
    cout << st.size() << endl;

    return 0;
}
```