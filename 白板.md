---
data: 2026-02-22
---
```cpp
#include<iostream>
using namespace std;
#include<vector>
vector<int>bestx;
vector<int>x;//记录子集中选择的元素
int arr[] = { 12,6,7,11,16,3,8,4 };
int length=sizeof(arr)/sizeof(arr[0]);
int u_sum=0;
int sum=0;
int m_min=0xFFFFFFFF;
int m_left;//未处理的节点的个数
int cnt++;//记录到达叶子节点的次数

void func(int i)
{
	if(i==length)
	{
		cnt++;
		if(x.size()!=length/2)
		{
			return;
		}
		int result=abs(sum-u_sum)
		if(result<m_min)
		{
			m_min=result;
			bestx=x;
		}
	}
	else
	{
		m_left--;
		if(x.size()<length/2)
		{
			sum+=arr[i];
			u_sum-=arr[i];
			x.emplace_back(arr[i]);
			func(i+1);
			x.pop_back(arr[i]);
			sum-=arr[i];
			u_sum+=arr[i];
		}
		if(x.size()+m_left>=length/2)
		{
			func(i+1);
		}		
		//还会回溯到其父节点的，因此m_left还要变回来
		m_left++;
	}
}
int main(){
	for(int x:arr)
	{
		u_sum+=arr;
	}
	func(0);
	for(int x:bsetx)
	{
		cout<<x<<" ";
	}
	cout<<endl;
	cout<<"min:"<<m_min<<endl;
	cout<<"cnt:"<<cnt<<endl;
	return 0;
}
```