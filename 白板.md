---
data: 2026-02-22
---
```cpp
#include <iostream>
#include <vector>
#include <string>
using namespace std;

/**
 * @brief 获取字符串pattern的部分匹配表（next数组，0-based）
 * @param pattern 模式串（0-based）
 * @return next数组，next[i]表示pattern[0..i]的最长公共前后缀长度
 */
vector<int> kmpNext(const string& pattern) {
    int n = pattern.size();
    vector<int> next(n, 0);  // 初始化next数组，全为0
    int len = 0;             // 当前子串的最长公共前后缀长度（对应Java里的len）
    int i = 1;               // 从第2个字符开始遍历（对应Java里的i=1）

    while (i < n) {
        if (pattern[len] == pattern[i]) {
            // 字符匹配：公共前后缀长度+1，记录到next[i]
            len++;
            next[i] = len;
            i++;
        } else {
            if (len == 0) {
                // 没有公共前后缀，next[i]为0，继续下一个字符
                next[i] = 0;
                i++;
            } else {
                // 不匹配时，回溯到更短的公共前后缀
                len = next[len - 1];
            }
        }
    }
    return next;
}

// 测试代码（和你Java里的测试用例一致）
int main() {
    string pattern = "ABCDABD";
    vector<int> next = kmpNext(pattern);
    
    cout << "next数组：";
    for (int num : next) {
        cout << num << " ";
    }
    // 输出：0 0 0 0 1 2 0，和你Java里的测试结果完全一致
    return 0;
}
