---
data: 2026-02-22
---

```cpp
#include <iostream>
#include <string>
#include <vector>
#include <set>
using namespace std;

int main() {
    string s;
    cin >> s;
    int n = s.size();
    if (n == 0) {
        cout << 0 << endl;
        return 0;
    }

    // 1. 预处理 f 数组：从 i 开始的最长合法子串长度
    vector<int> f(n, 1);
    for (int i = n - 2; i >= 0; --i) {
        if (s[i+1] == s[i] || s[i+1] == s[i] + 1) {
            f[i] = f[i+1] + 1;
        } else {
            f[i] = 1;
        }
    }

    // 2. 预处理 g 数组：到 i 结束的最长合法子串长度
    vector<int> g(n, 1);
    for (int i = 1; i < n; ++i) {
        if (s[i] == s[i-1] || s[i] == s[i-1] + 1) {
            g[i] = g[i-1] + 1;
        } else {
            g[i] = 1;
        }
    }

    // 3. 用集合记录所有合法子串 [l, r]，避免重复
    set<pair<int, int>> good;

    for (int k = 0; k < n - 1; ++k) {
        int left_l = k - g[k] + 1; // 左子串起点范围
        int left_r = k;
        int right_l = k + 1;
        int right_r = k + f[k+1]; // 右子串终点范围
        if (right_r >= n) right_r = n - 1;

        for (int l = left_l; l <= left_r; ++l) {
            for (int r = right_l; r <= right_r; ++r) {
                good.insert({l, r});
            }
        }
    }

    long long ans = n + good.size(); // 长度1 + 长度>1的好串
    cout << ans << endl;

    return 0;
}
```
