---
data: 2026-02-22
---
```cpp
#include <iostream>
#include <vector>
#include <unordered_map>
using namespace std;

const int MAXN = 2e5 + 10;

vector<pair<int, int>> tree1[MAXN]; // 第一棵树：邻接表，存储 (子节点编号, 权值)
vector<pair<int, int>> tree2[MAXN]; // 第二棵树：邻接表，存储 (子节点编号, 权值)
unordered_map<int, int> child2[MAXN]; // 第二棵树：快速查找，key=权值, value=子节点编号

int ans = 0;

void dfs(int u1, int u2, int len) {
    // 更新最大答案
    ans = max(ans, len);
    
    // 遍历第一棵树当前节点的所有子节点
    for (auto &edge : tree1[u1]) {
        int v1 = edge.first;
        int w1 = edge.second;
        
        // 在第二棵树中查找权值为w1的子节点
        if (child2[u2].count(w1)) {
            int v2 = child2[u2][w1];
            // 找到匹配，继续递归，匹配长度+1
            dfs(v1, v2, len + 1);
        }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n, m;
    cin >> n >> m;
    
    vector<int> val1(n+1), val2(m+1);
    for (int i = 1; i <= n; ++i) cin >> val1[i];
    for (int i = 1; i <= m; ++i) cin >> val2[i];
    
    // 构建第一棵树
    for (int i = 0; i < n-1; ++i) {
        int u, v;
        cin >> u >> v;
        // 注意：我们需要知道每个子节点的权值，所以存储 (v, val1[v])
        tree1[u].emplace_back(v, val1[v]);
        tree1[v].emplace_back(u, val1[u]);
    }
    
    // 构建第二棵树，并为每个节点建立“权值->子节点”的快速查找表
    for (int i = 0; i < m-1; ++i) {
        int p, q;
        cin >> p >> q;
        tree2[p].emplace_back(q, val2[q]);
        tree2[q].emplace_back(p, val2[p]);
        child2[p][val2[q]] = q;
        child2[q][val2[p]] = p;
    }
    
    // 如果根节点权值相同，才开始匹配
    if (val1[1] == val2[1]) {
        dfs(1, 1, 1);
    }
    
    cout << ans << endl;
    
    return 0;
}
```