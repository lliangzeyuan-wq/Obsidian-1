🎉 太棒了！你已经成功登录到服务器了，接下来跟着我一步步配置，就能把你的项目部署上线！

---

### 第一步：更新系统并安装基础依赖

直接在终端里复制下面的命令执行，把系统和需要的工具都装好：

bash

运行

```
# 更新软件源并升级系统
sudo apt update && sudo apt upgrade -y

# 安装编译工具、MySQL客户端和服务器依赖
sudo apt install g++ make libmysqlclient-dev mysql-server -y

# 安装nlohmann json库（和你本地用的一样）
sudo wget https://github.com/nlohmann/json/releases/download/v3.11.3/json.hpp -O /usr/include/json.hpp
```

---

### 第二步：配置 MySQL 数据库（和本地环境保持一致）

1. 设置 MySQL root 密码（替换成你自己的密码，比如和本地一样）：
    
    bash
    
    运行
    
    ```
    sudo mysql -u root
    ```
    
2. 在 MySQL 里执行下面的命令，设置密码并创建数据库：
    
    sql
    
    ```
    -- 设置root密码（替换成你自己的密码）
    ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '你的MySQL密码';
    FLUSH PRIVILEGES;
    
    -- 创建和本地一样的数据库
    CREATE DATABASE IF NOT EXISTS partner;
    USE partner;
    
    -- 退出MySQL
    exit;
    ```


---

### 第三步：上传你的代码文件

你需要把本地的两个东西传到服务器里：

- `server.cpp`（后端代码）
- `campus-social-frontend`（前端文件夹）

最简单的方法是用 `scp` 命令，在**你本地电脑的终端**里执行（替换成你的服务器 IP 和文件路径）：

bash

运行

```
# 上传后端代码
scp /本地路径/server.cpp admin@你的服务器公网IP:/home/admin/

# 上传前端文件夹（-r 表示递归上传整个文件夹）
scp -r /本地路径/campus-social-frontend admin@你的服务器公网IP:/home/admin/
```

---

### 第四步：编译并启动后端服务

回到服务器终端，执行下面的命令：

bash

运行

```
# 进入你的用户目录
cd /home/admin

# 编译后端代码（注意替换你的MySQL密码）
# 先修改server.cpp里的数据库密码为服务器的密码，然后编译
g++ server.cpp -o server -lmysqlclient

# 后台运行后端服务（关闭终端也不会停止）
nohup ./server &
```

---

### 第五步：启动前端服务

bash

运行

```
# 进入前端文件夹
cd /home/admin/campus-social-frontend

# 后台启动前端服务（端口3001）
nohup python3 -m http.server 3001 &
```

---

### 第六步：放行服务器端口

1. 回到阿里云控制台，找到你的服务器，点击【防火墙模板】
2. 添加两条规则：
    
    - 端口：`8080`，协议：`TCP`（后端服务）
    - 端口：`3001`，协议：`TCP`（前端网页）
    

---

✅ 现在你的项目已经完全上线了！

打开浏览器，访问 `http://你的服务器公网IP:3001`，就能看到你的校园社交网站了！

如果过程中遇到任何报错